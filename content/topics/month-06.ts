import type { Topic } from '../types';

/** Месяц 6, недели 21–24: модуль 3, качество кода, другие предметные области. */
export const MONTH_06_TOPICS: Topic[] = [
  {
    id: 'code-quality',
    title: 'Качество программного кода',
    tech: ['ts', 'react', 'express'],
    monthNo: 6,
    weekNo: 21,
    importance: 'core',
    summary:
      'Модуль 3 прямо требует «обеспечить высокое качество программного кода». Это не абстракция: есть конкретный список того, на что смотрят.',
    mustKnow: [
      'Маленькие компоненты и функции',
      'Отсутствие копипасты',
      'Никакого any в TypeScript',
      'Запросы собраны в одном месте',
      'Понятные имена и мёртвый код',
    ],
    theory: `## Что конкретно смотрят

Требование звучит общо, но раскладывается на проверяемые пункты:

| Пункт | Как проверить |
|---|---|
| Нет \`any\` | Поиск по проекту: \`: any\` |
| Запросы в папке \`api/\` | Поиск \`fetch(\` вне этой папки |
| Нет копипасты | Одинаковые блоки в двух и более местах |
| Компоненты небольшие | Файл больше 200 строк — повод разделить |
| Понятные имена | \`data\`, \`item2\`, \`temp\` — плохо |
| Нет закомментированного кода | Он есть в истории Git |
| Нет \`console.log\` в готовом коде | Отладочный вывод убирают |

## Размер файла

Ориентир: компонент до 150 строк, функция до 30. Если больше — почти всегда внутри прячется отдельная сущность.

Страница админки на 300 строк разбивается естественно: \`AdminFilters\`, \`AdminTable\`, \`AdminRow\`, \`Pagination\`. Каждый кусок понятен сам по себе.

## Копипаста

Три одинаковых поля формы — повод вынести компонент \`Field\`. Три одинаковых блока \`try/catch\` вокруг \`fetch\` — повод завести \`ApiClient\`.

Практическое правило: **третье повторение** — сигнал выносить. Второе ещё терпимо.

## Имена

\`\`\`ts
// плохо
const d = new Date();
function proc(a) { … }
const arr2 = arr.filter(…);

// хорошо
const startDate = new Date();
function formatApplicationDate(iso: string) { … }
const newApplications = applications.filter(…);
\`\`\`

Имя должно отвечать на вопрос «что это», а не «какого типа». \`applicationsList\` хуже, чем \`applications\`.

## Мёртвый код

Закомментированные куски, неиспользуемые импорты, файлы «на будущее» — всё убирается. История сохранена в Git, вернуть можно в любой момент.

## Форматирование

\`Shift+Alt+F\` в VS Code форматирует файл. Пройдитесь по всем файлам перед сдачей: единый отступ и расстановка скобок заметно влияют на впечатление.

## Что не является качеством

Не гонитесь за «идеальной архитектурой»: слои, абстрактные фабрики и универсальные хуки на четырёхчасовом экзамене только мешают. Качество здесь — это **читаемость, отсутствие дублирования и предсказуемость**.

## Быстрая самопроверка за 10 минут

\`\`\`text
1. Поиск по проекту: any        → ноль результатов
2. Поиск: console.log           → только осознанные
3. Поиск: fetch(                → только в api/
4. Открыть три самых больших файла → нет ли лишнего
5. Отформатировать все файлы
6. Проверить, что проект запускается с нуля по README
\`\`\``,
    examples: [
      {
        title: 'До и после: вынос повторяющегося поля',
        language: 'tsx',
        code: `// Было: три почти одинаковых блока
<div className="mb-3">
  <label htmlFor="login" className="form-label">Логин</label>
  <input id="login" name="login" className={\`form-control \${errors.login ? 'is-invalid' : ''}\`}
         value={values.login} onChange={handleChange} />
  {errors.login && <div className="invalid-feedback">{errors.login}</div>}
</div>
{/* …и так ещё четыре раза */}

// Стало: один компонент
interface FieldProps {
  name: string;
  label: string;
  type?: string;
  value: string;
  error?: string;
  onChange: (event: React.ChangeEvent<HTMLInputElement>) => void;
}

export function Field({ name, label, type = 'text', value, error, onChange }: FieldProps) {
  return (
    <div className="mb-3">
      <label htmlFor={name} className="form-label">{label}</label>
      <input
        id={name}
        name={name}
        type={type}
        className={\`form-control \${error ? 'is-invalid' : ''}\`}
        value={value}
        onChange={onChange}
      />
      {error && <div className="invalid-feedback">{error}</div>}
    </div>
  );
}

// Использование
<Field name="login" label="Логин" value={values.login} error={errors.login} onChange={handleChange} />`,
        explanation:
          'Пять полей превращаются в пять строк. Добавить шестое — ещё одна строка. Это и есть отсутствие копипасты в понятном виде.',
      },
    ],
    mistakes: [
      {
        title: 'Оставить any «чтобы работало»',
        why: 'Прямой пункт критерия качества. Поиск по проекту находит его за секунду.',
      },
      {
        title: 'Закомментированный код в сдаваемой работе',
        why: 'Выглядит как незаконченная работа. История в Git и так всё помнит.',
      },
      {
        title: 'Переусложнять ради «архитектуры»',
        why: 'Три слоя абстракции на четырёхчасовом проекте читаются хуже, чем прямой код.',
      },
    ],
    quizId: 'quiz-code-quality',
    taskIds: ['task-q-api-client'],
    projectIds: ['project-06-hardening'],
    resources: [],
    examRefs: ['m3-quality'],
    prerequisites: ['react-structure', 'server-architecture'],
    estimatedMinutes: 45,
    planDays: ['day-21-3'],
    source: 'plan',
  },

  {
    id: 'security-basics',
    title: 'Безопасность: закрыть дыры перед сдачей',
    tech: ['security'],
    monthNo: 6,
    weekNo: 21,
    importance: 'core',
    summary:
      'День 4 недели 21 — пройтись по проекту и закрыть все дыры. Пять пунктов, каждый из которых легко проверить.',
    mustKnow: [
      'Параметры во всех SQL-запросах',
      'Хеш пароля и что не уходит клиенту',
      'Проверка токена и роли на сервере',
      'Проверка «это твоя запись»',
      'XSS: текст пользователя не становится разметкой',
    ],
    theory: `## Пять проверок

### 1. SQL-инъекции

\`\`\`bash
# ищем склейку строк в запросах
grep -rn "query(\`" server/
grep -rn "execute(\`.*\\\${" server/
\`\`\`

Любое место, где значение попадает в SQL через шаблонную строку, — дыра. Должно быть только \`execute(sql, [параметры])\`.

Исключение — имена столбцов для сортировки: их подставляют из белого списка, а не из запроса.

### 2. Пароли

- в базе только хеш (\`password_hash\`, а не \`password\`);
- \`SELECT *\` в маршрутах, которые отдают пользователя, — проверить, не уходит ли хеш клиенту;
- пароль не пишется в лог.

\`\`\`bash
grep -rn "SELECT \\*" server/
\`\`\`

### 3. Токен и роль

Каждый защищённый маршрут должен проходить через \`requireAuth\`, каждый админский — ещё и через \`requireAdmin\`. Проверьте список маршрутов глазами: незащищённый админский маршрут — самая дорогая ошибка.

### 4. «Это твоя запись»

Вход проверен — этого мало. Действия над конкретной записью должны проверять владельца:

\`\`\`js
WHERE id = ? AND user_id = ?
\`\`\`

Проверьте маршруты отзыва и просмотра заявки: подставив чужой id, пользователь не должен ничего получить.

### 5. XSS

Отзывы и ФИО вводит пользователь. Если вставить их через \`innerHTML\`, чужой скрипт выполнится на вашей странице и прочитает токен из \`localStorage\`.

В React это безопасно по умолчанию: \`{review.text}\` вставляется как текст. Опасен только \`dangerouslySetInnerHTML\` — его в проекте быть не должно.

\`\`\`bash
grep -rn "dangerouslySetInnerHTML" client/src/
grep -rn "innerHTML" client/src/
\`\`\`

## Проверка за 15 минут

\`\`\`text
1. grep по склейке SQL                  → ничего
2. grep по SELECT * в маршрутах ответа  → ничего лишнего
3. Список маршрутов: у каждого своя защита
4. Попробовать открыть чужую заявку по id → 404
5. Оставить отзыв к чужой заявке        → 404
6. Открыть /api/admin/applications без токена → 401
7. grep по dangerouslySetInnerHTML      → ничего
\`\`\`

## Что не требуется

Для учебного проекта не нужны: защита от CSRF, ограничение частоты запросов, двухфакторная аутентификация, шифрование базы. Пять пунктов выше закрывают то, что действительно проверяют.`,
    examples: [
      {
        title: 'Проверка защиты запросом из консоли',
        language: 'javascript',
        code: `// Выполнить в консоли браузера, войдя обычным пользователем

// 1. Админский маршрут без прав
await fetch('/api/admin/applications', {
  headers: { Authorization: 'Bearer ' + localStorage.getItem('token') },
}).then((r) => r.status);
// Ожидается: 403

// 2. Админский маршрут вообще без токена
await fetch('/api/admin/applications').then((r) => r.status);
// Ожидается: 401

// 3. Чужая заявка
await fetch('/api/applications/999', {
  headers: { Authorization: 'Bearer ' + localStorage.getItem('token') },
}).then((r) => r.status);
// Ожидается: 404

// 4. Отзыв к чужой заявке
await fetch('/api/applications/999/review', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: 'Bearer ' + localStorage.getItem('token'),
  },
  body: JSON.stringify({ text: 'тест' }),
}).then((r) => r.status);
// Ожидается: 404

// 5. Регистрация с некорректными данными
await fetch('/api/register', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ login: 'ab', password: '1' }),
}).then((r) => r.status);
// Ожидается: 400`,
        explanation:
          'Пять запросов проверяют то, что нельзя увидеть в интерфейсе. Если хоть один вернул не тот код — есть что чинить.',
      },
    ],
    mistakes: [
      {
        title: 'Считать, что скрытая кнопка защищает',
        why: 'Запрос отправляется напрямую. Защищает только сервер.',
      },
      {
        title: 'SELECT * в ответе клиенту',
        why: 'Вместе с пользователем уходит хеш пароля.',
      },
      {
        title: 'Проверять вход, но не владельца',
        why: 'Любой вошедший сможет работать с чужими данными, подставив другой id.',
      },
    ],
    quizId: 'quiz-security-basics',
    taskIds: ['task-auth-roles', 'task-sec-sql-params', 'task-sec-ownership'],
    projectIds: ['project-06-hardening'],
    resources: [
      { title: 'OWASP Top 10', url: 'https://owasp.org/www-project-top-ten/', kind: 'reference', source: 'docs' },
    ],
    examRefs: ['m3-quality'],
    prerequisites: ['auth-roles', 'sql-insert'],
    estimatedMinutes: 50,
    planDays: ['day-21-4'],
    source: 'plan',
  },

  {
    id: 'ui-states',
    title: 'Состояния интерфейса: загрузка, пусто, ошибка',
    tech: ['react', 'design'],
    monthNo: 6,
    weekNo: 21,
    importance: 'core',
    summary:
      'У каждой страницы четыре состояния. Пустой экран вместо объяснения выглядит как поломка — и на это смотрят при оценке качества.',
    mustKnow: [
      'Четыре состояния каждой страницы',
      'Понятный текст вместо технической ошибки',
      'Кнопка «Повторить»',
      'Блокировка кнопки во время отправки',
      'Пустое состояние с подсказкой, что делать',
    ],
    theory: `## Четыре состояния

\`\`\`tsx
if (loading) return <Spinner />;
if (error) return <ErrorBlock message={error} onRetry={load} />;
if (!items.length) return <EmptyState />;
return <List items={items} />;
\`\`\`

Порядок именно такой. Лесенка ранних возвратов читается лучше вложенных условий.

## Загрузка

Подойдёт спиннер Bootstrap:

\`\`\`tsx
<div className="text-center py-5">
  <div className="spinner-border" role="status">
    <span className="visually-hidden">Загрузка…</span>
  </div>
</div>
\`\`\`

Текст внутри \`visually-hidden\` нужен программам чтения с экрана.

## Ошибка

\`\`\`tsx
<div className="alert alert-danger d-flex justify-content-between align-items-center">
  <span>Не удалось загрузить заявки. Проверьте соединение.</span>
  <button className="btn btn-sm btn-outline-danger" onClick={onRetry}>Повторить</button>
</div>
\`\`\`

Два правила:

1. **Понятный текст**, а не \`TypeError: Cannot read properties of undefined\`.
2. **Действие**: кнопка «Повторить» или подсказка, что делать.

Техническую ошибку пишут в \`console.error\` — она нужна разработчику, а не пользователю.

## Пусто

\`\`\`tsx
<div className="alert alert-light border text-center">
  <p className="mb-2">У вас пока нет заявок</p>
  <Link className="btn btn-primary btn-sm" to="/order">Оформить заявку</Link>
</div>
\`\`\`

Хорошее пустое состояние не только сообщает «пусто», но и подсказывает следующий шаг.

## Во время действия

\`\`\`tsx
<button className="btn btn-primary" disabled={sending}>
  {sending ? 'Отправляем…' : 'Отправить заявку'}
</button>
\`\`\`

Без блокировки двойной клик создаст две одинаковые заявки — это видно в базе и портит впечатление.

## Где нужны состояния

- кабинет (список заявок);
- страница заявки (загрузка справочников);
- админка (список всех заявок);
- любая форма (состояние отправки).

## Быстрая проверка

Откройте DevTools → Network → Throttling → Slow 3G и перезагрузите страницу. Вы увидите состояние загрузки, которое обычно проскакивает незаметно. Затем переключите на Offline и обновите — должна появиться ошибка с кнопкой «Повторить».`,
    examples: [
      {
        title: 'Переиспользуемые компоненты состояний',
        language: 'tsx',
        code: `export function Loading({ text = 'Загрузка…' }: { text?: string }) {
  return (
    <div className="text-center py-5">
      <div className="spinner-border" role="status">
        <span className="visually-hidden">{text}</span>
      </div>
    </div>
  );
}

export function ErrorBlock({ message, onRetry }: { message: string; onRetry?: () => void }) {
  return (
    <div className="alert alert-danger d-flex justify-content-between align-items-center gap-2" role="alert">
      <span>{message}</span>
      {onRetry && (
        <button type="button" className="btn btn-sm btn-outline-danger" onClick={onRetry}>
          Повторить
        </button>
      )}
    </div>
  );
}

export function EmptyState({ text, actionText, actionHref }: {
  text: string; actionText?: string; actionHref?: string;
}) {
  return (
    <div className="alert alert-light border text-center">
      <p className="mb-2">{text}</p>
      {actionText && actionHref && (
        <Link className="btn btn-primary btn-sm" to={actionHref}>{actionText}</Link>
      )}
    </div>
  );
}`,
        explanation:
          'Три компонента на 30 строк закрывают состояния всех страниц проекта. Это и есть отсутствие копипасты в сочетании с требованием к качеству.',
      },
    ],
    mistakes: [
      {
        title: 'Пустой экран при загрузке',
        why: 'Пользователь думает, что приложение сломалось, и начинает нажимать кнопки повторно.',
      },
      {
        title: 'Показывать текст технической ошибки',
        why: 'Он ничего не объясняет пользователю и выглядит непрофессионально.',
      },
      {
        title: 'Не блокировать кнопку отправки',
        why: 'Двойной клик создаёт дубликат заявки — видно в базе.',
      },
    ],
    quizId: 'quiz-ui-states',
    taskIds: ['task-react-effect-load', 'task-ui-four-states'],
    resources: [],
    examRefs: ['m3-quality'],
    prerequisites: ['react-effects'],
    estimatedMinutes: 45,
    planDays: ['day-21-5'],
    source: 'plan',
  },

  {
    id: 'git-workflow',
    title: 'Ритм «этап → коммит»',
    tech: ['git'],
    monthNo: 6,
    weekNo: 21,
    importance: 'core',
    summary:
      'Задание требует минимум три коммита в каждом модуле. Правильный ритм даёт больше — и заодно страхует от потери работы.',
    mustKnow: [
      'Коммит после каждого завершённого этапа',
      'Осмысленные сообщения',
      'Что должно быть в истории к концу экзамена',
      'Откат при поломке',
      'Проверка истории перед сдачей',
    ],
    theory: `## Требование задания

В каждом из трёх модулей: «Обязательно выполняйте промежуточные фиксации изменений приложения (минимум три «коммита») перед каждым новым этапом работы». То есть минимум девять коммитов за экзамен.

## Ритм

\`\`\`text
Заработал этап → git add . → git commit -m "..." → следующий этап
\`\`\`

Не «в конце сделаю коммит», а сразу. Это занимает пять секунд и даёт точку возврата.

## История, которую стоит показать

\`\`\`text
Модуль 1
  Старт проекта: .gitignore
  Каркас: client, server, database
  БД: схема, справочники, ER-диаграмма
  Сервер: подключение к MySQL, регистрация и вход
  Клиент: формы регистрации и входа
  Заявка: форма и запись в БД
  Кабинет: свои заявки и отзывы
  Админка: список заявок и смена статуса

Модуль 2
  Слайдер: 4 изображения, автопереключение 3 секунды
  Формы: подсказки об ошибках рядом с полями
  Админка: фильтр, сортировка, пагинация, уведомления
  Мобильная версия: 390x844
  Единое оформление: отступы, цвета, иконки

Модуль 3
  БД: внешние ключи и ограничения
  Безопасность: параметризованные запросы, проверка ролей
  Состояния интерфейса: загрузка, ошибка, пусто
  Микроанимации и prefers-reduced-motion
  README и финальная проверка
\`\`\`

Такая история сама по себе показывает ход работы — это работает на оценку.

## Сообщения

Плохо: \`fix\`, \`upd\`, \`работа\`, \`1\`.

Хорошо: что сделано, в одной строке, без точки в конце.

\`\`\`bash
git commit -m "Админка: фильтр по статусу и пагинация"
\`\`\`

## Откат

\`\`\`bash
git status                  # что изменилось
git restore файл            # вернуть файл к последнему коммиту
git restore .               # вернуть всё (осторожно!)
git log --oneline           # найти нужный коммит
\`\`\`

На экзамене не используйте \`reset --hard\`: он удаляет незакоммиченные изменения без возможности вернуть.

## Проверка перед сдачей

\`\`\`bash
git status                  # чисто, ничего не забыто
git log --oneline | wc -l   # коммитов достаточно
git log --oneline           # сообщения читаемы
\`\`\`

Убедитесь, что \`node_modules\` не попал в историю: \`git ls-files | grep node_modules\` должен ничего не вывести.`,
    examples: [
      {
        title: 'Аварийное восстановление',
        language: 'bash',
        code: `# Сломали рабочий код за 20 минут до конца модуля

git status                       # смотрим, что изменено
git diff                         # что именно поменялось

# Вариант 1: вернуть один файл
git restore client/src/pages/AdminPage.tsx

# Вариант 2: вернуть всё к последнему коммиту
git restore .

# Вариант 3: сохранить текущее состояние и вернуться к рабочему
git stash                        # спрятать изменения
# ... проверили, что всё работает ...
git stash pop                    # вернуть изменения обратно

git log --oneline                # убедиться, что история на месте`,
        explanation:
          'Именно ради таких ситуаций и нужны частые коммиты: потерять можно только работу после последнего коммита.',
      },
    ],
    mistakes: [
      {
        title: 'Один коммит в конце модуля',
        why: 'Формально требование не выполнено, и страховки от поломки нет.',
      },
      {
        title: 'Коммитить сломанный код',
        why: 'Точка возврата должна быть рабочей. Сначала проверьте, что запускается.',
      },
      {
        title: 'reset --hard в панике',
        why: 'Удаляет незакоммиченные изменения безвозвратно. Используйте restore или stash.',
      },
    ],
    quizId: 'quiz-git-workflow',
    taskIds: [],
    resources: [
      { title: 'learngitbranching.js.org', url: 'https://learngitbranching.js.org/?locale=ru_RU', kind: 'practice', source: 'plan' },
    ],
    examRefs: ['m1-git', 'm2-git', 'm3-git'],
    prerequisites: ['git-basics'],
    estimatedMinutes: 35,
    planDays: ['day-21-6'],
    source: 'plan',
  },

  {
    id: 'new-domains',
    title: 'Новая предметная область за четыре часа',
    tech: ['react', 'express', 'sql'],
    monthNo: 6,
    weekNo: 22,
    importance: 'core',
    summary:
      'На экзамене может попасться другой вариант. Умение быстро перенести готовый скелет на новую тему важнее, чем знание одного проекта наизусть.',
    mustKnow: [
      'Что меняется от варианта к варианту',
      'Что остаётся неизменным',
      'Порядок разбора нового задания',
      'Где обычно прячутся отличия',
      'Как тренироваться',
    ],
    theory: `## Что не меняется

Практически всё:

- структура проекта (client, server, database);
- схема базы: пользователи, справочник, заявки, отзывы;
- регистрация, вход, JWT, роли;
- личный кабинет и админка;
- фильтр, сортировка, пагинация;
- слайдер, адаптив, валидация.

## Что меняется

- названия сущностей (помещения → курсы → услуги);
- набор полей справочника (у курса есть часы, у услуги — цена);
- тексты статусов;
- логин и пароль администратора;
- точные формулировки ссылок.

## Разбор нового задания за 10 минут

\`\`\`text
1. Выписать существительные          → таблицы
2. Найти ТОЧНЫЕ строки:
   - логин и пароль администратора
   - названия статусов
   - текст ссылки между входом и регистрацией
   - размер экрана и параметры слайдера
3. Отметить, чего в задании НЕТ      → придумать и упомянуть
4. Сверить со своим скелетом         → что добавить
\`\`\`

Пункт 2 — самый важный: именно эти строки проверяют дословно.

## Где прячутся отличия

**Число статусов.** В одном варианте три, в другом может быть четыре или другая цепочка переходов.

**Кто оставляет отзыв.** Может быть привязан не к заявке, а к услуге.

**Дополнительные поля.** Количество мест, длительность, стоимость — появляются в справочнике.

**Роли.** Может добавиться третья роль (например, мастер или преподаватель).

## Как тренироваться

Программа предлагает три полных проекта на разных темах (недели 22–23) и прогоны на новых темах в месяце 7. Темы для тренировки:

- запись на курсы;
- запись в автосервис;
- клининговые услуги;
- аренда рабочих мест;
- запись к врачу.

Генератор тренировочных вариантов в этой платформе собирает задания по тем же предметным областям.

## Цель

К концу месяца 6: новая тема не вызывает паузы. Вы открываете задание, за 10 минут выписываете сущности и точные строки, и дальше работаете по отработанному скелету.`,
    examples: [
      {
        title: 'Разбор задания «Запись в автосервис»',
        language: 'text',
        code: `Существительные → таблицы:
  клиент (users), услуга (services), способ оплаты (payment_methods),
  запись (appointments), отзыв (reviews)

Точные строки (выписать буква в букву):
  - логин/пароль администратора
  - статусы: «Новая», «Принята в работу», «Выполнена»
  - текст ссылки между входом и регистрацией
  - размер экрана, параметры слайдера

Отличия от «Конференции.РФ»:
  - у услуги есть длительность и цена → два поля в справочнике
  - статусов по-прежнему три, но названия другие → константы поменять

Чего нет в задании:
  - перечень способов оплаты → придумать 2-3 и упомянуть проверяющему

План: тот же скелет, 20 минут на БД, дальше по отработанному порядку.`,
      },
    ],
    mistakes: [
      {
        title: 'Зубрить один проект',
        why: 'Вариантов несколько. Программа прямо советует тренироваться на разных темах.',
      },
      {
        title: 'Не выписать точные строки',
        why: 'Статусы и текст ссылки проверяют дословно. По памяти «примерно так» не работает.',
      },
      {
        title: 'Проектировать базу заново',
        why: 'Скелет один и тот же. Меняются только названия и пара полей.',
      },
    ],
    quizId: 'quiz-new-domains',
    taskIds: ['task-sql-schema-courses'],
    resources: [],
    examRefs: ['m1-db', 'm1-admin'],
    prerequisites: ['db-modeling', 'admin-panel'],
    estimatedMinutes: 45,
    planDays: ['day-22-1', 'day-23-1'],
    source: 'plan',
  },

  {
    id: 'memory-training',
    title: 'Всё из головы: тренировка на скорость',
    tech: ['tools'],
    monthNo: 6,
    weekNo: 24,
    importance: 'core',
    summary:
      'Неделя 24 целиком про скорость: каждый кусок проекта пишется по памяти на время. Цель — убрать паузы «как это было».',
    mustKnow: [
      'Что нужно знать наизусть',
      'Нормативы времени по частям',
      'Как тренироваться по частям',
      'Чем можно пользоваться на экзамене',
      'Как замерять прогресс',
    ],
    theory: `## Что должно быть в голове

Учебная программа даёт конкретный список:

**Схема БД** — пять таблиц с ключами и ограничениями, 10–15 минут по памяти.

**Список маршрутов API** — девять адресов с методами.

**Каркас клиента** — структура папок, main.tsx, App.tsx с маршрутами.

**Ключевые компоненты** — Header, ProtectedRoute, Slider, DateInput, Field, Toast.

**Валидация** — регулярка логина, правила пароля, формат даты.

**Точные строки задания** — статусы, логин админа, текст ссылки.

## Нормативы недели 24

\`\`\`text
БД + ER-диаграмма + каркас двух проектов    25 мин
Регистрация + вход full stack               30 мин
Заявка + кабинет + отзывы                   30 мин
Админка с фильтром, сортировкой, пагинацией 30 мин
Слайдер + валидация + мобильная версия      40 мин
\`\`\`

В сумме около 2,5 часов — это и есть модуль 1 плюс половина модуля 2. Запас нужен: на экзамене всегда что-то идёт не так.

## Как тренироваться

Не переписывайте проект целиком каждый раз — это долго и скучно. Тренируйте **по частям**:

1. Пустая папка, таймер, одна задача (например, «регистрация full stack»).
2. Пишете по памяти, не подсматривая никуда. Застряли — отметили и пошли дальше.
3. Записываете время и что тормозило.
4. На следующий день начинаете с того, что тормозило.

## Что будет под рукой на экзамене

Ничего. Ни интернета, ни своих файлов, ни записей — только компьютер с редактором и выданные материалы задания. Поэтому проверка готовности всего одна: сесть и написать нужный кусок по памяти.

Записи во время подготовки всё равно полезны, но не как справочник на экзамен, а как список того, что нужно повторить. Держите в нём:

- схему БД;
- список маршрутов;
- регулярки и правила валидации;
- структуру папок;
- точные строки из задания;
- свой личный список «где я обычно торможу».

К дню экзамена каждый пункт этого списка должен вспоминаться без самого списка.

## Замер прогресса

Ведите короткий журнал:

\`\`\`text
Дата     Задача                      Время   Что тормозило
12.02    Регистрация full stack      48 мин  Вспоминал синтаксис middleware
19.02    Регистрация full stack      37 мин  Опечатка в имени поля
26.02    Регистрация full stack      29 мин  —
\`\`\`

Сокращение времени и исчезновение пометок в последнем столбце — и есть готовность.`,
    examples: [
      {
        title: 'Шаблон тренировки одной части',
        language: 'text',
        code: `Задача: админка с фильтром, сортировкой и пагинацией
Цель: 30 минут

0:00  Создать страницу AdminPage, добавить маршрут
0:03  Запрос на сервере: JOIN по трём справочникам
0:10  Таблица на клиенте: загрузка и вывод
0:16  Смена статуса через PATCH
0:21  Фильтр по статусу
0:25  Сортировка кликом по заголовку
0:28  Пагинация по 5 записей
0:30  Коммит

После: записать фактическое время и три пункта,
на которых пришлось вспоминать.`,
      },
    ],
    mistakes: [
      {
        title: 'Тренироваться только целыми прогонами',
        why: 'Четыре часа на прогон — можно сделать один раз в неделю. Части тренируются каждый день.',
      },
      {
        title: 'Подсматривать в готовый проект',
        why: 'На экзамене не будет ни проекта, ни записей. Если кусок не пишется без подсказки, он пока не выучен.',
      },
      {
        title: 'Не записывать, где тормозил',
        why: 'Без списка слабых мест тренировка превращается в повторение того, что уже получается.',
      },
    ],
    quizId: 'quiz-memory-training',
    taskIds: ['task-exam-auth-middleware-speed', 'task-exam-date-speed', 'task-exam-schema-speed', 'task-exam-slider-speed'],
    resources: [],
    examRefs: [],
    prerequisites: ['new-domains'],
    estimatedMinutes: 40,
    planDays: ['day-24-1'],
    source: 'plan',
  },
];
