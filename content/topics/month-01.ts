import type { Topic } from '../types';

/**
 * Месяц 1 (недели 1–4): HTML, CSS, адаптив, Git, VS Code.
 * Темы привязаны к дням программы через planDays.
 */
export const MONTH_01_TOPICS: Topic[] = [
  {
    id: 'web-basics',
    title: 'Как устроен веб: браузер, сервер, HTTP',
    tech: ['tools'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'Браузер отправляет HTTP-запрос, сервер отвечает данными или страницей. Всё приложение экзамена — это две программы (клиент и сервер), которые общаются запросами.',
    mustKnow: [
      'Что делает браузер, а что сервер',
      'Из чего состоит HTTP-запрос и ответ',
      'Что такое код ответа: 200, 400, 401, 404, 500',
      'Чем фронтенд отличается от бэкенда и где живёт база данных',
      'Что такое API простыми словами',
    ],
    theory: `## Клиент и сервер

Когда вы открываете страницу, происходит одно и то же:

1. Браузер (клиент) отправляет **запрос** по адресу.
2. Сервер получает запрос, что-то делает (читает базу данных, считает) и отправляет **ответ**.
3. Браузер получает ответ и рисует то, что пришло.

Браузер не умеет ходить в базу данных напрямую. Между ними всегда стоит сервер — это важно на экзамене: React сам по себе не может сделать \`SELECT\`, он просит об этом Express.

## Запрос

Запрос состоит из четырёх частей:

| Часть | Пример | Зачем |
|---|---|---|
| Метод | \`GET\`, \`POST\`, \`PATCH\`, \`DELETE\` | Что делаем: читаем, создаём, меняем, удаляем |
| Адрес | \`/api/applications\` | Куда обращаемся |
| Заголовки | \`Authorization: Bearer …\` | Служебная информация: кто мы, какой формат |
| Тело | \`{"roomId": 2}\` | Данные (у GET его обычно нет) |

## Ответ

У ответа есть **код состояния** — короткое число, по которому сразу понятно, что случилось:

- **200** — всё хорошо;
- **201** — создано (после регистрации или новой заявки);
- **400** — запрос неправильный (не прошла валидация);
- **401** — не авторизован (нет токена или он неверный);
- **403** — доступ запрещён (вы не администратор);
- **404** — не найдено;
- **409** — конфликт (логин уже занят);
- **500** — сломался сервер.

Эти коды понадобятся уже в месяце 4, когда будете писать Express. На экзамене «информативные уведомления при неправильном входе» — это и есть грамотная обработка 400/401.

## Где что лежит

\`\`\`text
Браузер                  Сервер                     База данных
─────────                ──────                     ───────────
React + TypeScript  ←→   Node.js + Express     ←→   MySQL
страницы, формы          маршруты /api/...          таблицы
проверка на клиенте      проверка на сервере        хранение
\`\`\`

**Правило, которое спасает баллы:** клиенту доверять нельзя. Проверку логина и пароля пишут дважды — на фронте (чтобы пользователю было удобно) и на сервере (чтобы систему нельзя было обойти).

## API

API — это список адресов, которые понимает сервер, и правила обращения к ним. Для проекта экзамена он выглядит примерно так:

\`\`\`text
POST /api/register              регистрация
POST /api/login                 вход
GET  /api/rooms                 список помещений
GET  /api/applications/my       мои заявки
POST /api/applications          создать заявку
GET  /api/admin/applications    все заявки (только админ)
PATCH /api/admin/applications/5 сменить статус
\`\`\`

Этот список вы составите сами на неделе 15 — программа просит принести его на проверку.`,
    examples: [
      {
        title: 'Что видно во вкладке Network',
        language: 'text',
        code: `Request URL: http://localhost:3000/api/login
Request Method: POST
Status Code: 401 Unauthorized

Request Body:
{"login":"ivanov01","password":"qwerty"}

Response Body:
{"error":"Неверный логин или пароль"}`,
        explanation:
          'Код 401 и понятное сообщение — это ровно то, что требует задание: «Неправильные попытки входа сопровождаются информативными уведомлениями».',
      },
    ],
    practicalExample: {
      title: 'Путь одной заявки на экзамене',
      language: 'text',
      code: `1. Пользователь заполняет форму заявки в React
2. Фронт проверяет поля (помещение выбрано, дата корректна)
3. fetch('/api/applications', { method: 'POST', body: JSON.stringify(data) })
4. Express проверяет токен, проверяет поля ещё раз
5. INSERT INTO applications (..., status) VALUES (..., 'Новая')
6. Ответ 201 + созданная заявка
7. React показывает уведомление «Заявка отправлена»`,
      explanation: 'Запомните эту цепочку: по ней строятся почти все требования модуля 1.',
    },
    mistakes: [
      {
        title: 'Думать, что React «сам» сохранит данные',
        why: 'React живёт в браузере и после перезагрузки страницы теряет состояние. Сохраняет только сервер и база данных.',
      },
      {
        title: 'Проверять данные только на клиенте',
        wrong: 'Валидация в форме — и сразу INSERT на сервере',
        right: 'Валидация в форме + повторная проверка в маршруте Express',
        why: 'Запрос можно отправить в обход формы. На экзамене за это снимают баллы по пункту «качество кода».',
      },
      {
        title: 'Путать 401 и 403',
        why: '401 — «я не знаю, кто вы» (нет токена). 403 — «я знаю, кто вы, но вам сюда нельзя» (не админ).',
      },
    ],
    quizId: 'quiz-web-basics',
    taskIds: [],
    resources: [
      {
        title: 'MDN: Обзор HTTP',
        url: 'https://developer.mozilla.org/ru/docs/Web/HTTP/Overview',
        kind: 'docs',
        source: 'docs',
      },
      {
        title: 'doka.guide — как работает интернет',
        url: 'https://doka.guide/tools/how-the-internet-works/',
        kind: 'tutorial',
        source: 'plan',
        note: 'Ресурс из списка «Где читать теорию» учебной программы',
      },
    ],
    examRefs: ['m1-register', 'm1-login'],
    prerequisites: [],
    estimatedMinutes: 35,
    planDays: ['day-01-1'],
    source: 'plan',
  },

  {
    id: 'tools-vscode',
    title: 'VS Code и Emmet: редактор как подсказчик без интернета',
    tech: ['tools'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'На экзамене нет интернета, зато есть VS Code. Emmet, автодополнение и быстрые исправления заменяют документацию и экономят десятки минут.',
    mustKnow: [
      'Emmet: `!`, `div.card>h5+p`, `ul>li*3`',
      'Ctrl+Space — автодополнение, Ctrl+. — быстрое исправление и автоимпорт',
      'Ctrl+P, Ctrl+Shift+F, F2, Shift+Alt+F, Ctrl+`',
      'Панель Source Control для коммитов мышкой',
      'Почему стоит иногда работать с `code --disable-extensions`',
    ],
    theory: `## Почему это отдельная тема

На демоэкзамене нет интернета. Значит, нет Google, нет Stack Overflow и нет документации в браузере. Остаётся один источник подсказок — **редактор**. Всё остальное должно быть в голове. Учебная программа прямо требует выучить VS Code так же серьёзно, как код.

## Emmet — самая большая экономия времени

Emmet встроен в VS Code и работает в \`.html\` и в \`.tsx\`. Вы пишете короткую строку, нажимаете **Tab**, и получаете разметку.

| Сокращение | Что получится |
|---|---|
| \`!\` | Полный каркас HTML5 с \`meta charset\` и \`viewport\` |
| \`div.card\` | \`<div class="card"></div>\` |
| \`ul>li*3\` | Список из трёх пунктов |
| \`div.row>div.col*2\` | Сетка Bootstrap из двух колонок |
| \`input:text\` | \`<input type="text">\` |
| \`table>thead>tr>th*4\` | Заголовок таблицы заявок |
| \`.badge.bg-primary{Новая}\` | \`<span class="badge bg-primary">Новая</span>\` |

Точка — это класс, решётка — идентификатор, \`>\` — вложенность, \`+\` — соседний элемент, \`*\` — повторение, фигурные скобки — текст внутри.

В TSX Emmet тоже работает, но помните: там \`className\`, а не \`class\`. VS Code подставит правильный вариант, если файл имеет расширение \`.tsx\`.

## Автодополнение вместо документации

TypeScript «знает» типы и показывает их прямо в редакторе:

- **Ctrl+Space** — список доступных свойств и методов;
- наведение мышкой — тип и описание;
- красное подчёркивание — ошибка **до** запуска.

Именно поэтому на экзамене TypeScript выгоден: он превращает редактор в справочник. Если вы описали тип \`Application\`, то, набрав \`application.\`, увидите все поля — не нужно вспоминать, как называется колонка.

## Быстрые исправления

**Ctrl+.** — самое полезное сочетание. Оно умеет:

- добавить недостающий \`import\`;
- создать функцию, которой ещё нет;
- убрать неиспользуемую переменную.

Импорты в React-проекте руками писать почти не нужно.

## Горячие клавиши, которые должны быть в пальцах

\`\`\`text
Ctrl+P          открыть файл по имени
Ctrl+Shift+F    поиск по всему проекту
F2              переименовать во всех файлах сразу
Shift+Alt+F     отформатировать файл
Ctrl+D          выделить следующее такое же слово
Alt+↑ / Alt+↓   переместить строку
Ctrl+/          закомментировать
Ctrl+\`          открыть/закрыть терминал
\`\`\`

## Про расширения

На экзаменационном компьютере может не быть ваших любимых плагинов. Раз в пару недель запускайте \`code --disable-extensions\` и работайте без них — чтобы в день экзамена отсутствие Prettier не стало новостью.`,
    examples: [
      {
        title: 'Каркас страницы за одно нажатие',
        language: 'html',
        code: `<!-- набираем ! и жмём Tab -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>

</body>
</html>`,
        explanation: 'Сразу замените lang="en" на lang="ru" и напишите осмысленный title — это часть оценки качества.',
      },
      {
        title: 'Карточка заявки одной строкой Emmet',
        language: 'text',
        code: `.card.mb-3>.card-body>h5.card-title{Коворкинг}+p.card-text{14.09.2026}+span.badge.bg-secondary{Новая}`,
        explanation: 'Нажмите Tab — получите готовую карточку Bootstrap. На экзамене такие строки экономят минуты.',
      },
    ],
    mistakes: [
      {
        title: 'Писать разметку целиком руками',
        why: 'Модуль 1 длится 90 минут. Каждая минута на закрывающих тегах — это минута, не потраченная на функциональность.',
      },
      {
        title: 'Надеяться на расширения',
        why: 'Если на экзаменационной машине нет автоформатирования, придётся форматировать вручную — лучше знать об этом заранее.',
      },
    ],
    quizId: 'quiz-tools-vscode',
    taskIds: [],
    resources: [
      { title: 'Документация Emmet', url: 'https://docs.emmet.io/cheat-sheet/', kind: 'reference', source: 'docs' },
      {
        title: 'VS Code: горячие клавиши (Windows)',
        url: 'https://code.visualstudio.com/shortcuts/keyboard-shortcuts-windows.pdf',
        kind: 'reference',
        source: 'docs',
      },
    ],
    examRefs: [],
    prerequisites: ['web-basics'],
    estimatedMinutes: 30,
    planDays: ['day-01-2'],
    source: 'plan',
  },

  {
    id: 'html-structure',
    title: 'Структура HTML-документа и базовые теги',
    tech: ['html'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'Любая страница начинается с одного и того же каркаса: doctype, html, head с кодировкой и viewport, body. Без meta viewport не будет адаптива, который требует модуль 2.',
    mustKnow: [
      '`<!DOCTYPE html>`, `<html lang="ru">`, `<head>`, `<body>`',
      '`<meta charset="UTF-8">` — иначе кириллица превратится в кракозябры',
      '`<meta name="viewport" content="width=device-width, initial-scale=1">` — без неё телефон покажет «уменьшенный десктоп»',
      'Заголовки h1–h6: один h1 на страницу',
      'Текстовые теги: p, strong, em, br, hr',
    ],
    theory: `## Каркас

\`\`\`html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Конференции.РФ — вход</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <!-- видимое содержимое -->
</body>
</html>
\`\`\`

Разберём построчно.

**\`<!DOCTYPE html>\`** — говорит браузеру: «это современный HTML». Если забыть, браузер переключится в режим совместимости со старыми сайтами, и вёрстка поедет непредсказуемо.

**\`lang="ru"\`** — язык страницы. Влияет на переносы, проверку орфографии и на то, как читают страницу программы экранного доступа.

**\`<meta charset="UTF-8">\`** — кодировка. Ставьте **первой строкой** в \`head\`: браузер начинает разбирать файл до того, как дойдёт до конца, и должен узнать кодировку как можно раньше. Без неё русский текст выглядит как \`Ð—Ð°ÑÐ²ÐºÐ°\`.

**\`<meta name="viewport" …>\`** — самое важное для экзамена. Без этой строки мобильный браузер считает, что страница шириной 980 px, и просто уменьшает её. Все ваши \`@media\` не сработают, и требование «совместимость с экраном 390×844» не будет выполнено.

**\`<title>\`** — текст во вкладке браузера. Не оставляйте \`Document\` из шаблона Emmet.

## Заголовки

\`h1\` — самый важный заголовок, \`h6\` — самый мелкий. Правила:

- один \`h1\` на страницу (это название страницы);
- уровни не перепрыгивают: после \`h2\` идёт \`h3\`, а не \`h5\`;
- заголовок выбирают по **смыслу**, а не по размеру шрифта — размер задаётся в CSS.

## Текст

\`\`\`html
<p>Обычный абзац.</p>
<p><strong>Важно:</strong> статус новой заявки — <em>Новая</em>.</p>
<hr>
\`\`\`

- \`<p>\` — абзац;
- \`<strong>\` — важность (жирный по умолчанию);
- \`<em>\` — смысловое выделение (курсив по умолчанию);
- \`<br>\` — перенос строки, применять редко;
- \`<hr>\` — тематический разделитель.

\`<b>\` и \`<i>\` тоже дают жирный и курсив, но не несут смысла. Предпочитайте \`strong\` и \`em\`.

## Комментарии

\`\`\`html
<!-- Блок заявок пользователя -->
\`\`\`

Комментарии видны всем, кто откроет исходный код. Не оставляйте в них пароли и заметки вроде «тут костыль».`,
    examples: [
      {
        title: 'Минимальная страница входа',
        language: 'html',
        code: `<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Вход — Конференции.РФ</title>
</head>
<body>
  <header>
    <h1>Конференции.РФ</h1>
  </header>
  <main>
    <h2>Вход в систему</h2>
    <p>Введите логин и пароль, чтобы продолжить.</p>
  </main>
</body>
</html>`,
        runnable: true,
      },
    ],
    mistakes: [
      {
        title: 'Забыть meta viewport',
        wrong: '<head><meta charset="UTF-8"><title>…</title></head>',
        right: '<head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>…</title></head>',
        why: 'Без viewport требование модуля 2 «совместимость с экраном 390×844» не выполняется, как бы хорошо ни были написаны медиазапросы.',
      },
      {
        title: 'Несколько h1 на странице',
        why: 'Структура заголовков — это оглавление документа. Несколько h1 делают его бессмысленным.',
      },
      {
        title: 'Оставить title = Document',
        why: 'Мелочь, но её видно в первой же секунде проверки работы.',
      },
    ],
    quizId: 'quiz-html-structure',
    taskIds: ['task-html-skeleton'],
    resources: [
      {
        title: 'MDN: структура HTML-документа',
        url: 'https://developer.mozilla.org/ru/docs/Learn/HTML/Introduction_to_HTML/Document_and_website_structure',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-mobile', 'm2-layouts'],
    prerequisites: ['tools-vscode'],
    estimatedMinutes: 40,
    planDays: ['day-01-2'],
    source: 'plan',
  },

  {
    id: 'html-links-images',
    title: 'Ссылки, изображения и списки',
    tech: ['html'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'Ссылка — основной способ навигации, а её текст на экзамене проверяют дословно. Изображения нужны для слайдера модуля 2, списки — для меню и требований.',
    mustKnow: [
      '`<a href="...">` и разница между относительным и абсолютным путём',
      'Атрибут `alt` у изображения обязателен',
      '`ul`/`ol`/`li` и вложенные списки',
      '`target="_blank"` и зачем к нему `rel="noopener"`',
      'Ссылка внутри React — это `<Link>`, а не `<a>`',
    ],
    theory: `## Ссылки

\`\`\`html
<a href="register.html">Еще не зарегистрированы? Регистрация</a>
\`\`\`

Этот пример не случайный: задание экзамена требует ссылку **ровно с таким текстом**. Проверяющий сверяет формулировку буквально, поэтому не «Зарегистрироваться», не «Ещё не зарегистрированы?» — а точная строка из задания.

### Пути

| Вид | Пример | Когда |
|---|---|---|
| Относительный | \`href="register.html"\` | Файл рядом |
| Вложенный | \`href="pages/login.html"\` | Файл в подпапке |
| На уровень выше | \`href="../index.html"\` | Файл в родительской папке |
| От корня | \`href="/login"\` | Маршрут приложения |
| Абсолютный | \`href="https://example.com"\` | Внешний сайт |

Якорь \`href="#form"\` прокручивает к элементу с \`id="form"\`, а \`href="mailto:…"\` и \`href="tel:…"\` открывают почту и звонилку — пригодится в контактах.

### Внешние ссылки

\`\`\`html
<a href="https://getbootstrap.com" target="_blank" rel="noopener noreferrer">Bootstrap</a>
\`\`\`

\`target="_blank"\` открывает новую вкладку, \`rel="noopener"\` закрывает старую уязвимость, при которой открытая страница получала доступ к вашей через \`window.opener\`.

## Изображения

\`\`\`html
<img src="img/hall-1.jpg" alt="Конференц-зал на 100 мест" width="600" height="400">
\`\`\`

- \`src\` — путь к файлу;
- \`alt\` — текст, который увидит пользователь, если картинка не загрузилась, и который прочитает скринридер. Пустой \`alt=""\` допустим только для декоративных картинок;
- \`width\`/\`height\` — размеры в пикселях. Указывать их полезно: браузер заранее резервирует место, и страница не «прыгает» при загрузке.

Для слайдера модуля 2 требуется **четыре одинаковых по размерам изображения**. Одинаковый размер проще всего обеспечить не в редакторе картинок, а в CSS — через \`object-fit: cover\` (это тема «Изображения в CSS»).

## Списки

\`\`\`html
<ul>
  <li>Аудитория</li>
  <li>Коворкинг</li>
  <li>Кинозал</li>
</ul>

<ol>
  <li>Зарегистрироваться</li>
  <li>Оформить заявку</li>
  <li>Дождаться статуса</li>
</ol>
\`\`\`

\`ul\` — когда порядок не важен, \`ol\` — когда важен. Внутри \`li\` можно вкладывать что угодно, в том числе другой список.

Навигационное меню — это почти всегда \`ul\` внутри \`nav\`: так поступают и Bootstrap, и большинство реальных сайтов.`,
    examples: [
      {
        title: 'Навигация из списка',
        language: 'html',
        code: `<nav>
  <ul>
    <li><a href="/">Главная</a></li>
    <li><a href="/order">Оформить заявку</a></li>
    <li><a href="/cabinet">Личный кабинет</a></li>
  </ul>
</nav>`,
      },
      {
        title: 'Пара ссылок из задания экзамена',
        language: 'html',
        code: `<!-- На странице входа -->
<a href="/register">Еще не зарегистрированы? Регистрация</a>

<!-- На странице регистрации — обратная ссылка -->
<a href="/login">Уже зарегистрированы? Вход</a>`,
        explanation:
          'Текст первой ссылки задан заданием дословно. Текст обратной ссылки в задании не указан — формулировка на ваше усмотрение, но она обязана быть.',
      },
    ],
    mistakes: [
      {
        title: 'Пропустить alt',
        wrong: '<img src="hall.jpg">',
        right: '<img src="hall.jpg" alt="Кинозал на 60 мест">',
        why: 'Это прямое нарушение доступности и первое, что замечают при проверке качества вёрстки.',
      },
      {
        title: 'Написать текст ссылки «по смыслу»',
        wrong: '<a href="/register">Регистрация</a>',
        right: '<a href="/register">Еще не зарегистрированы? Регистрация</a>',
        why: 'Задание содержит точную формулировку. Это самый дешёвый балл на экзамене — и самый обидный, если его потерять.',
      },
      {
        title: 'Ссылка-заглушка href="#"',
        why: 'Раздел 45 требований: кнопка, которая никуда не ведёт, выглядит рабочей, но не работает. Либо реализуйте переход, либо не рисуйте элемент.',
      },
    ],
    quizId: 'quiz-html-links',
    taskIds: ['task-html-nav'],
    resources: [
      {
        title: 'MDN: элемент a',
        url: 'https://developer.mozilla.org/ru/docs/Web/HTML/Element/a',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m1-login', 'm2-slider'],
    prerequisites: ['html-structure'],
    estimatedMinutes: 35,
    planDays: ['day-01-3'],
    source: 'plan',
  },

  {
    id: 'git-basics',
    title: 'Git: репозиторий, коммиты, история',
    tech: ['git'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'Задание экзамена требует репозиторий и минимум три коммита в каждом модуле. Это не формальность: коммит — единственный способ вернуться, если что-то сломалось на 70-й минуте.',
    mustKnow: [
      '`git init`, `git status`, `git add`, `git commit -m`, `git log --oneline`',
      'Что такое коммит и почему он делается перед новым этапом',
      '`.gitignore` с `node_modules`',
      'Панель Source Control в VS Code',
      '`git restore` — откат случайных правок',
    ],
    theory: `## Зачем это на экзамене

В каждом из трёх модулей задание требует: «Обязательно выполняйте промежуточные фиксации изменений приложения (минимум три «коммита») перед каждым новым этапом работы». То есть минимум **девять** коммитов за экзамен.

Практическая польза важнее формальной: если вы сломали работающий код за 20 минут до конца модуля, коммит позволяет вернуться к рабочей версии одной командой.

## Минимальный набор команд

\`\`\`bash
git init                      # создать репозиторий в текущей папке
git status                    # что изменилось
git add .                     # подготовить все изменения
git add server/index.js       # подготовить один файл
git commit -m "Регистрация: форма и валидация"
git log --oneline             # короткая история
\`\`\`

Один раз на компьютере нужно представиться:

\`\`\`bash
git config --global user.name "Иван Иванов"
git config --global user.email "ivanov@example.com"
\`\`\`

## Три состояния файла

\`\`\`text
Рабочая папка  →  git add  →  Индекс (staged)  →  git commit  →  История
   изменили                    подготовили                        зафиксировали
\`\`\`

\`git status\` всегда показывает, в каком состоянии что находится. Если запутались — начните с него.

## Сообщения коммитов

Плохо: \`fix\`, \`123\`, \`работа\`.

Хорошо:

\`\`\`text
Каркас проекта: client, server, database
БД: схема и тестовые данные
Регистрация: форма, валидация, запись в БД
Вход: JWT и защищённые маршруты
Админка: список заявок и смена статуса
\`\`\`

Такая история сама по себе показывает проверяющему ход работы — это плюс к оценке.

## .gitignore

\`\`\`text
node_modules/
.env
dist/
\`\`\`

\`node_modules\` — это десятки тысяч файлов. Если закоммитить их, коммит будет идти минуту, а репозиторий станет нечитаемым. Файл \`.gitignore\` создаётся **до** первого коммита.

## Ритм работы

Программа предлагает довести до рефлекса: **этап → коммит**.

\`\`\`text
Сделал БД          → коммит
Сделал регистрацию → коммит
Сделал вход        → коммит
Сделал кабинет     → коммит
\`\`\`

Не «в конце модуля закоммичу всё» — именно по ходу. Если время закончится, у вас всё равно будет история из нескольких коммитов, а требование задания — выполнено.

## Откат

\`\`\`bash
git restore file.js           # вернуть файл к последнему коммиту
git restore --staged file.js  # убрать из индекса, изменения оставить
git log --oneline             # найти нужный коммит
\`\`\`

На экзамене не экспериментируйте с \`reset --hard\`: команда удаляет незакоммиченные изменения безвозвратно.`,
    examples: [
      {
        title: 'Начало работы над модулем 1',
        language: 'bash',
        code: `mkdir conference && cd conference
git init
printf "node_modules/\\n.env\\n" > .gitignore
git add .gitignore
git commit -m "Старт проекта: .gitignore"`,
      },
      {
        title: 'История, которую хочется увидеть у проверяющего',
        language: 'bash',
        code: `$ git log --oneline
9f1c2ab Админка: фильтр, сортировка, пагинация
4b77e10 Админка: смена статуса заявки
f0a3d55 Кабинет: мои заявки и отзывы
7c21e98 Заявка: форма и запись в БД
2d9b410 Вход: JWT и защищённые маршруты
a51f733 Регистрация: валидация и запись в БД
1e0c8d2 БД: схема и тестовые данные
0a4b219 Старт проекта: .gitignore`,
      },
    ],
    mistakes: [
      {
        title: 'Один коммит в конце модуля',
        why: 'Задание требует промежуточные фиксации «перед каждым новым этапом». Один коммит — невыполненное требование.',
      },
      {
        title: 'Закоммитить node_modules',
        why: 'Репозиторий раздувается до сотен мегабайт, история становится нечитаемой. Создайте .gitignore до первого add.',
      },
      {
        title: 'Коммитить только в самом конце дня подготовки',
        why: 'Программа предлагает завершать коммитом каждый учебный день — чтобы на экзамене это было рефлексом, а не задачей.',
      },
    ],
    quizId: 'quiz-git-basics',
    taskIds: [],
    resources: [
      { title: 'Pro Git (на русском)', url: 'https://git-scm.com/book/ru/v2', kind: 'docs', source: 'docs' },
      {
        title: 'learngitbranching.js.org',
        url: 'https://learngitbranching.js.org/?locale=ru_RU',
        kind: 'practice',
        source: 'plan',
        note: 'Тренажёр из списка учебной программы',
      },
    ],
    examRefs: ['m1-git', 'm2-git', 'm3-git'],
    prerequisites: [],
    estimatedMinutes: 40,
    planDays: ['day-01-4'],
    source: 'plan',
  },

  {
    id: 'html-semantic',
    title: 'Семантические теги и таблицы',
    tech: ['html'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'Семантика делает разметку понятной и доступной, а таблица — основной способ показать все заявки в панели администратора.',
    mustKnow: [
      'header, nav, main, section, article, aside, footer',
      'Чем div отличается от section и когда div уместен',
      'table, thead, tbody, tr, th, td, caption',
      'scope="col" у заголовков столбцов',
      'Почему таблицу на телефоне нужно оборачивать в контейнер со скроллом',
    ],
    theory: `## Семантика

Семантические теги — это те же блоки, что \`div\`, но с понятным назначением:

\`\`\`html
<body>
  <header>       <!-- шапка: логотип, меню -->
    <nav>…</nav>
  </header>

  <main>         <!-- основное содержимое, одно на страницу -->
    <section>…</section>
  </main>

  <footer>…</footer>
</body>
\`\`\`

| Тег | Смысл |
|---|---|
| \`header\` | Шапка страницы или блока |
| \`nav\` | Навигация |
| \`main\` | Главное содержимое, ровно одно на страницу |
| \`section\` | Смысловой раздел с заголовком |
| \`article\` | Самостоятельная единица (карточка заявки, отзыв) |
| \`aside\` | Побочное содержимое (фильтры, подсказки) |
| \`footer\` | Подвал |

\`div\` не запрещён — он нужен там, где блок создан **только для стилей** и не несёт смысла (обёртка для flex-раскладки, например).

Зачем это на экзамене: критерий «качество кода» и доступность. Скринридер умеет переходить «к основному содержимому», если есть \`main\`, и перечислять разделы навигации, если есть \`nav\`.

## Таблицы

Таблица нужна для данных, а не для раскладки страницы (так делали двадцать лет назад — сейчас это ошибка).

\`\`\`html
<table>
  <caption>Все заявки</caption>
  <thead>
    <tr>
      <th scope="col">№</th>
      <th scope="col">Помещение</th>
      <th scope="col">Дата</th>
      <th scope="col">Статус</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Коворкинг</td>
      <td>14.09.2026</td>
      <td>Новая</td>
    </tr>
  </tbody>
</table>
\`\`\`

- \`thead\` — шапка, \`tbody\` — тело; браузер и стили работают с ними по-разному;
- \`th\` — ячейка-заголовок, \`td\` — обычная;
- \`scope="col"\` говорит, что заголовок относится к столбцу (важно для доступности);
- \`caption\` — подпись таблицы, читается первой.

## Таблица на телефоне

Экран 390 px узкий, а колонок в админке много: ФИО, телефон, помещение, дата, оплата, статус. Два рабочих подхода:

1. **Горизонтальный скролл.** Обернуть таблицу в блок с \`overflow-x: auto\` (в Bootstrap это класс \`table-responsive\`).
2. **Превращение в карточки.** В медиазапросе показать каждую строку как карточку с подписями полей.

Оба варианта засчитываются; первый быстрее, второй выглядит лучше. На экзамене при нехватке времени берите первый.`,
    examples: [
      {
        title: 'Каркас страницы админки',
        language: 'html',
        code: `<body>
  <header>
    <nav><a href="/admin">Заявки</a> <a href="/logout">Выход</a></nav>
  </header>
  <main>
    <h1>Панель администратора</h1>
    <section aria-label="Фильтры">…</section>
    <div class="table-responsive">
      <table>…</table>
    </div>
  </main>
  <footer><p>Конференции.РФ, 2026</p></footer>
</body>`,
      },
    ],
    mistakes: [
      {
        title: 'Вёрстка таблицами',
        why: 'Раскладку делают flexbox и grid. Таблица — только для табличных данных.',
      },
      {
        title: 'Несколько main на странице',
        why: 'main должен быть один: это «основное содержимое документа».',
      },
      {
        title: 'Таблица шире экрана без обёртки',
        why: 'На 390 px появляется горизонтальная прокрутка всей страницы — прямое нарушение требования модуля 2.',
      },
    ],
    quizId: 'quiz-html-semantic',
    taskIds: ['task-html-table', 'task-html-nav'],
    projectIds: ['project-01-event-page'],
    resources: [
      {
        title: 'MDN: семантические элементы',
        url: 'https://developer.mozilla.org/ru/docs/Glossary/Semantics',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m1-admin', 'm2-mobile'],
    prerequisites: ['html-structure'],
    estimatedMinutes: 40,
    planDays: ['day-01-5'],
    source: 'plan',
  },

  {
    id: 'tools-devtools',
    title: 'DevTools: Elements, Console и режим устройства 390×844',
    tech: ['tools'],
    monthNo: 1,
    weekNo: 1,
    importance: 'core',
    summary:
      'DevTools — это способ увидеть, что на самом деле происходит на странице: какие стили применились, какая ошибка в консоли, как выглядит вёрстка на 390×844.',
    mustKnow: [
      'F12 или Ctrl+Shift+I — открыть панель',
      'Elements: дерево страницы и применённые стили',
      'Console: ошибки JavaScript и вывод console.log',
      'Ctrl+Shift+M — режим устройства, ввод размера 390×844 вручную',
      'Network: запросы, коды ответа, тела запроса и ответа',
    ],
    theory: `## Elements

Вкладка Elements показывает **итоговое** дерево страницы — то, что получилось после работы JavaScript, а не то, что написано в файле. Это важно: React рисует разметку динамически, и увидеть её можно только здесь.

Справа — панель стилей:

- **Styles** — какие правила применились и откуда;
- зачёркнутое свойство — его перебило другое правило (тема «Каскад и специфичность»);
- **Computed** — итоговое значение каждого свойства;
- схема блочной модели внизу — реальные margin, border, padding, content в пикселях.

Свойства можно менять прямо в браузере: удобно подбирать отступы, не перезапуская сборку. Изменения не сохраняются — не забудьте перенести их в CSS-файл.

## Console

Здесь видны:

- ошибки JavaScript (красные) — обязательно читайте текст и имя файла со строкой;
- предупреждения React (жёлтые) — например, знаменитое «Each child in a list should have a unique key»;
- ваш \`console.log\`.

Полезные функции: \`console.table(массив)\` рисует массив объектов таблицей — идеально для списка заявок.

## Режим устройства

**Ctrl+Shift+M** включает эмуляцию устройства. Сверху появляется поле размеров: выберите **Responsive** и введите **390 × 844** — это размер из задания (iPhone 12/13/14).

На что смотреть:

1. Нет ли горизонтальной прокрутки.
2. Помещается ли таблица.
3. Не слипаются ли кнопки (минимальный удобный размер касания — около 44 px).
4. Читается ли текст без увеличения.

## Network

Вкладка Network показывает каждый запрос: адрес, метод, код ответа, время, тело. Когда фронт «не видит» данные, ответ всегда здесь: либо 401 (забыли токен), либо 404 (опечатка в адресе), либо 500 (упал сервер — смотрите терминал).

Фильтр **Fetch/XHR** оставляет только запросы к API и убирает картинки и стили.

## Application

Здесь лежит \`localStorage\` — именно в нём будет храниться токен входа. Если вход «не запоминается», проверьте, появился ли ключ с токеном.`,
    examples: [
      {
        title: 'Быстрая проверка мобильной вёрстки',
        language: 'text',
        code: `1. F12 → Ctrl+Shift+M
2. Выбрать Responsive, ввести 390 × 844
3. Прокрутить страницу вниз до конца
4. Проверить: нет горизонтальной полосы прокрутки
5. Повторить для всех пяти страниц проекта`,
        explanation: 'Такую проверку программа предлагает делать в конце каждого дня вёрстки.',
      },
    ],
    mistakes: [
      {
        title: 'Не открывать консоль',
        why: 'Половина «непонятных» проблем — это ошибка, уже написанная в консоли красным текстом.',
      },
      {
        title: 'Проверять адаптив, просто сужая окно',
        why: 'Окно браузера не уходит в 390 px на большинстве мониторов, и масштаб отличается. Используйте режим устройства.',
      },
    ],
    quizId: 'quiz-tools-devtools',
    taskIds: [],
    resources: [
      {
        title: 'Chrome DevTools: обзор',
        url: 'https://developer.chrome.com/docs/devtools/overview',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-mobile', 'm3-mobile'],
    prerequisites: ['html-structure'],
    estimatedMinutes: 30,
    planDays: ['day-01-6'],
    source: 'plan',
  },

  {
    id: 'html-forms',
    title: 'Формы: поля, типы, встроенная валидация',
    tech: ['html'],
    monthNo: 1,
    weekNo: 2,
    importance: 'core',
    summary:
      'Три формы задания — регистрация, вход и заявка — целиком состоят из элементов этой темы. Правильные типы полей и атрибуты дают часть валидации бесплатно.',
    mustKnow: [
      '`<form>`, `<input>`, `<label for>`, `<button type="submit">`',
      'Типы: text, password, email, tel, date, number, radio, checkbox',
      'Атрибуты: name, required, minlength, pattern, placeholder',
      '`<select>`/`<option>` и `<textarea>`',
      'Почему у каждого поля должен быть name и label',
    ],
    theory: `## Каркас формы

\`\`\`html
<form action="/api/register" method="post">
  <label for="login">Логин</label>
  <input type="text" id="login" name="login" required minlength="6" pattern="[A-Za-z0-9]{6,}">

  <button type="submit">Зарегистрироваться</button>
</form>
\`\`\`

- \`action\` — куда уйдут данные, \`method\` — каким методом. В React-приложении оба атрибута обычно не нужны: отправку перехватывает JavaScript.
- \`label for="login"\` связан с \`id="login"\`. Благодаря связи клик по подписи ставит курсор в поле — и это требование доступности.
- \`name\` — имя поля. Без него значение не попадёт в отправку формы.

## Типы полей

| Тип | Для чего | Что даёт |
|---|---|---|
| \`text\` | Логин, ФИО | Обычная строка |
| \`password\` | Пароль | Скрывает символы |
| \`email\` | Почта | Проверяет наличие @ и точки |
| \`tel\` | Телефон | На телефоне открывает цифровую клавиатуру |
| \`date\` | Дата | Календарь браузера |
| \`number\` | Количество | Только числа, стрелки |
| \`radio\` | Один из вариантов | Группируется общим \`name\` |
| \`checkbox\` | Да/нет или набор | Значение в \`value\` |

Для формы регистрации из задания нужны ровно пять полей: логин (\`text\`), пароль (\`password\`), ФИО (\`text\`), телефон (\`tel\`), e-mail (\`email\`).

## Встроенная валидация

Браузер умеет проверять часть правил сам:

\`\`\`html
<input type="password" name="password" required minlength="8">
<input type="text" name="login" required pattern="[A-Za-z0-9]{6,}"
       title="Только латинские буквы и цифры, минимум 6 символов">
\`\`\`

- \`required\` — поле обязательно;
- \`minlength\` / \`maxlength\` — длина;
- \`pattern\` — регулярное выражение;
- \`title\` — подсказка, которую браузер покажет в сообщении об ошибке.

**Но:** встроенных сообщений браузера недостаточно для экзамена. Модуль 2 требует «показывать соответствующие подсказки рядом с формой» — то есть свой текст ошибки под конкретным полем. Встроенная валидация остаётся как второй рубеж, а основную вы напишете на JavaScript (неделя 6) и на React (неделя 10).

## Выпадающие списки

\`\`\`html
<label for="room">Помещение</label>
<select id="room" name="roomId" required>
  <option value="">— выберите —</option>
  <option value="1">Аудитория</option>
  <option value="2">Коворкинг</option>
  <option value="3">Кинозал</option>
</select>
\`\`\`

Модуль 2 прямо требует выпадающие списки на странице заявки. Первый \`option\` с пустым \`value\` вместе с \`required\` заставляет пользователя сделать осознанный выбор.

\`textarea\` — многострочное поле, нужно для отзыва:

\`\`\`html
<textarea name="review" rows="4" maxlength="500"></textarea>
\`\`\`

## Кнопки

\`\`\`html
<button type="submit">Отправить</button>  <!-- отправляет форму -->
<button type="button">Сбросить фильтр</button>  <!-- ничего не делает сама -->
\`\`\`

Если у кнопки внутри формы не указать \`type\`, она считается \`submit\` и перезагружает страницу. Это классическая причина «форма сама отправляется, ничего не понимаю».`,
    examples: [
      {
        title: 'Форма регистрации из задания',
        language: 'html',
        code: `<form id="register-form" novalidate>
  <div>
    <label for="login">Логин</label>
    <input type="text" id="login" name="login" required minlength="6"
           pattern="[A-Za-z0-9]{6,}" placeholder="ivanov26">
    <small class="error" id="login-error"></small>
  </div>

  <div>
    <label for="password">Пароль</label>
    <input type="password" id="password" name="password" required minlength="8">
    <small class="error" id="password-error"></small>
  </div>

  <div>
    <label for="fio">ФИО</label>
    <input type="text" id="fio" name="fio" required>
  </div>

  <div>
    <label for="phone">Телефон</label>
    <input type="tel" id="phone" name="phone" required placeholder="+7 999 123-45-67">
  </div>

  <div>
    <label for="email">E-mail</label>
    <input type="email" id="email" name="email" required>
  </div>

  <button type="submit">Зарегистрироваться</button>
</form>
<a href="/login">Уже зарегистрированы? Вход</a>`,
        explanation:
          'Атрибут novalidate отключает встроенные сообщения браузера, чтобы показывать свои — те самые «подсказки рядом с формой».',
        runnable: true,
      },
    ],
    mistakes: [
      {
        title: 'Поле без label',
        wrong: '<input type="text" placeholder="Логин">',
        right: '<label for="login">Логин</label><input id="login" type="text">',
        why: 'placeholder исчезает при вводе и не читается скринридером. Он дополняет подпись, а не заменяет её.',
      },
      {
        title: 'Забыть name у поля',
        why: 'Без name значение не попадает ни в FormData, ни в обычную отправку формы.',
      },
      {
        title: 'Полагаться только на pattern и required',
        why: 'Модуль 2 требует свои подсказки рядом с формой, а сервер обязан перепроверить всё независимо от браузера.',
      },
    ],
    quizId: 'quiz-html-forms',
    taskIds: ['task-html-register-form'],
    projectIds: ['project-01-event-page'],
    resources: [
      {
        title: 'MDN: формы',
        url: 'https://developer.mozilla.org/ru/docs/Learn/Forms',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m1-register', 'm1-order', 'm2-order-form', 'm2-register-hints'],
    prerequisites: ['html-structure'],
    estimatedMinutes: 45,
    planDays: ['day-02-1', 'day-02-2'],
    source: 'plan',
  },

  {
    id: 'css-basics',
    title: 'CSS: подключение, селекторы, цвета и единицы',
    tech: ['css'],
    monthNo: 1,
    weekNo: 2,
    importance: 'core',
    summary:
      'CSS отвечает на вопрос «как это выглядит». Селектор выбирает элементы, объявление задаёт свойства. Единицы rem и % — основа адаптивности.',
    mustKnow: [
      'Подключение через `<link rel="stylesheet" href="style.css">`',
      'Селекторы: тег, .класс, #id, потомок, группа',
      'Цвета: имя, #hex, rgb(), hsl()',
      'Единицы: px, %, rem, em, vh, vw',
      'Переменные `--name` и `var(--name)`',
    ],
    theory: `## Подключение

\`\`\`html
<link rel="stylesheet" href="style.css">
\`\`\`

Тег ставится в \`head\`. Способ со \`<style>\` внутри страницы годится для быстрых экспериментов, а атрибут \`style="..."\` на элементе — только в крайнем случае: он перебивает почти всё и мешает поддерживать код.

## Синтаксис

\`\`\`css
.card {            /* селектор */
  padding: 16px;   /* свойство: значение; */
  border-radius: 8px;
}
\`\`\`

## Селекторы

| Селектор | Что выбирает |
|---|---|
| \`p\` | Все абзацы |
| \`.card\` | Элементы с классом card |
| \`#login\` | Элемент с id="login" |
| \`.card p\` | Абзацы **внутри** .card (любой глубины) |
| \`.card > p\` | Только прямые потомки |
| \`h1, h2\` | Группа: и заголовки первого, и второго уровня |
| \`input[type="password"]\` | По атрибуту |

В повседневной работе 90 % — это классы. Идентификаторы используйте для связи \`label for\` ↔ \`input id\`, а не для стилей: у них слишком высокий приоритет, и потом трудно что-то переопределить.

## Цвета

\`\`\`css
color: #1f2937;                 /* шестнадцатеричный */
color: rgb(31 41 55);           /* красный, зелёный, синий */
color: rgb(31 41 55 / 0.6);     /* с прозрачностью */
color: hsl(215 25% 27%);        /* тон, насыщенность, светлота */
\`\`\`

HSL удобен, когда нужен тот же цвет, но светлее: меняете третье число.

## Единицы

| Единица | Что значит | Когда |
|---|---|---|
| \`px\` | Пиксель | Границы, мелкие отступы |
| \`%\` | Доля от родителя | Ширина блоков |
| \`rem\` | Размер шрифта корня (обычно 16 px) | Отступы, шрифты — масштабируется вместе с настройками пользователя |
| \`em\` | Размер шрифта текущего элемента | Отступы внутри кнопки |
| \`vh\` / \`vw\` | 1 % высоты/ширины окна | Экраны на всю высоту |

Для адаптивной вёрстки главные — \`%\`, \`rem\` и \`fr\` (в Grid). Фиксированная ширина в пикселях — первая причина горизонтальной прокрутки на 390 px.

## Переменные

\`\`\`css
:root {
  --brand: #2563eb;
  --radius: 10px;
}

.btn {
  background: var(--brand);
  border-radius: var(--radius);
}
\`\`\`

Переменные экономят время на экзамене: поменяли один цвет в \`:root\` — обновилась вся тема. Это же лучший способ сделать тёмную тему, если останется время.`,
    examples: [
      {
        title: 'Стартовый файл стилей проекта',
        language: 'css',
        code: `:root {
  --brand: #2563eb;
  --text: #111827;
  --muted: #6b7280;
  --bg: #f9fafb;
  --radius: 10px;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font-family: system-ui, Arial, sans-serif;
  color: var(--text);
  background: var(--bg);
  line-height: 1.5;
}

.container {
  width: min(100% - 2rem, 1100px);
  margin-inline: auto;
}`,
        explanation:
          'Приём `width: min(100% - 2rem, 1100px)` даёт контейнер с отступами на телефоне и ограничением ширины на большом экране — без единого медиазапроса.',
      },
    ],
    mistakes: [
      {
        title: 'Стили в атрибуте style',
        why: 'Их невозможно переопределить обычным правилом и нельзя переиспользовать. На экзамене это минус к качеству кода.',
      },
      {
        title: 'Ширина в пикселях у блоков',
        wrong: '.card { width: 500px; }',
        right: '.card { width: 100%; max-width: 500px; }',
        why: 'На экране 390 px блок в 500 px вылезет за край и создаст горизонтальную прокрутку.',
      },
      {
        title: 'Стилизация по id',
        why: 'Специфичность id настолько высока, что потом приходится городить `!important`.',
      },
    ],
    quizId: 'quiz-css-basics',
    taskIds: ['task-css-selectors'],
    resources: [
      {
        title: 'MDN: первые шаги в CSS',
        url: 'https://developer.mozilla.org/ru/docs/Learn/CSS/First_steps',
        kind: 'docs',
        source: 'docs',
      },
      { title: 'doka.guide — CSS', url: 'https://doka.guide/css/', kind: 'tutorial', source: 'plan' },
    ],
    examRefs: ['m2-design', 'm2-mobile'],
    prerequisites: ['html-structure'],
    estimatedMinutes: 40,
    planDays: ['day-02-3'],
    source: 'plan',
  },

  {
    id: 'css-typography',
    title: 'Шрифты, текст, каскад и специфичность',
    tech: ['css'],
    monthNo: 1,
    weekNo: 2,
    importance: 'core',
    summary:
      'Читаемость — половина оценки за дизайн. Плюс каскад: правила, по которым браузер решает, какое из конфликтующих правил победит.',
    mustKnow: [
      'font-family со списком запасных шрифтов',
      'font-size, font-weight, line-height, letter-spacing',
      'text-align, text-transform, text-decoration',
      'Наследование: что наследуется, а что нет',
      'Специфичность: тег < класс < id < style < !important',
    ],
    theory: `## Шрифты

\`\`\`css
body {
  font-family: system-ui, -apple-system, "Segoe UI", Arial, sans-serif;
}
\`\`\`

Список читается слева направо: браузер берёт первый доступный шрифт. \`system-ui\` — системный шрифт устройства: он уже установлен, грузить ничего не надо и выглядит «родным».

**Важно для экзамена:** подключать шрифты с Google Fonts нельзя — интернета не будет. Либо системные шрифты, либо файл шрифта, положенный рядом с проектом и подключённый через \`@font-face\`.

## Размер и высота строки

\`\`\`css
body {
  font-size: 16px;
  line-height: 1.5;   /* без единиц — множитель от размера шрифта */
}
h1 { font-size: 2rem; }
\`\`\`

\`line-height\` без единиц — правильный выбор: наследуется как множитель и корректно работает при разных размерах шрифта. Комфортный диапазон для текста — 1.4–1.6.

Минимальный размер основного текста на мобильном — 16 px. Меньше — браузер на iOS начнёт увеличивать страницу при фокусе в поле ввода.

## Оформление текста

\`\`\`css
.muted { color: #6b7280; }
.title { font-weight: 600; letter-spacing: -0.01em; }
.badge { text-transform: uppercase; font-size: 12px; }
a { text-decoration: underline; }
\`\`\`

Подчёркивание у ссылок лучше оставлять: это единственный признак ссылки, не зависящий от цвета, и он важен для людей, не различающих цвета.

## Наследование

Часть свойств передаётся детям автоматически: \`color\`, \`font-*\`, \`line-height\`, \`text-align\`. Поэтому базовые настройки текста задают один раз на \`body\`.

Не наследуются: отступы, границы, фон, размеры. Их задают каждому элементу.

## Каскад: кто победит

Когда два правила задают одно свойство одному элементу, браузер выбирает по порядку:

1. **Важность:** \`!important\` перебивает всё (поэтому им почти не пользуются).
2. **Специфичность:** считается как «сколько id, сколько классов, сколько тегов»:
   - \`p\` → 0-0-1
   - \`.card p\` → 0-1-1
   - \`#login\` → 1-0-0
   - атрибут \`style="..."\` → сильнее любого селектора
3. **Порядок:** при равной специфичности выигрывает правило, написанное **ниже**.

\`\`\`css
p { color: gray; }          /* 0-0-1 */
.card p { color: black; }   /* 0-1-1 — победит */
\`\`\`

Практический вывод: держите специфичность низкой и однородной — тогда стили предсказуемы. Если приходится писать \`!important\`, почти всегда это признак, что где-то стоит лишний id или inline-стиль.

## Подключение Bootstrap и свои стили

Bootstrap подключается первым, ваш файл — вторым. Тогда ваши правила той же специфичности переопределят библиотечные, потому что идут ниже.`,
    examples: [
      {
        title: 'Типографика проекта',
        language: 'css',
        code: `body {
  font-family: system-ui, Arial, sans-serif;
  font-size: 16px;
  line-height: 1.5;
  color: #111827;
}

h1 { font-size: 1.75rem; line-height: 1.2; margin: 0 0 1rem; }
h2 { font-size: 1.35rem; line-height: 1.25; margin: 0 0 .75rem; }
.text-muted { color: #6b7280; font-size: .9rem; }
.error { color: #b91c1c; font-size: .85rem; }`,
      },
    ],
    mistakes: [
      {
        title: 'Подключить шрифт с Google Fonts',
        why: 'На экзамене нет интернета: шрифт не загрузится, страница покажется системным шрифтом, и «фирменный стиль» рассыплется.',
      },
      {
        title: 'line-height в пикселях',
        wrong: 'line-height: 24px;',
        right: 'line-height: 1.5;',
        why: 'При изменении размера шрифта фиксированная высота строки ломает текст.',
      },
      {
        title: 'Лечить конфликты через !important',
        why: 'Дальше каждое следующее исправление тоже потребует !important. Разберитесь со специфичностью.',
      },
    ],
    quizId: 'quiz-css-typography',
    taskIds: ['task-css-specificity'],
    resources: [
      {
        title: 'MDN: каскад и наследование',
        url: 'https://developer.mozilla.org/ru/docs/Learn/CSS/Building_blocks/Cascade_and_inheritance',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-design'],
    prerequisites: ['css-basics'],
    estimatedMinutes: 40,
    planDays: ['day-02-4'],
    source: 'plan',
  },

  {
    id: 'css-box-model',
    title: 'Блочная модель: width, padding, border, margin, box-sizing',
    tech: ['css'],
    monthNo: 1,
    weekNo: 2,
    importance: 'core',
    summary:
      'Каждый элемент — это прямоугольник из содержимого, внутренних отступов, рамки и внешних отступов. Одна строка `box-sizing: border-box` избавляет от половины проблем с размерами.',
    mustKnow: [
      'Порядок слоёв: content → padding → border → margin',
      'Разница между padding и margin',
      '`box-sizing: border-box` и почему его ставят всем элементам',
      'Схлопывание вертикальных margin',
      'display: block, inline, inline-block, none',
    ],
    theory: `## Четыре слоя

\`\`\`text
┌─────────── margin ───────────┐   внешний отступ (между элементами)
│ ┌───────── border ─────────┐ │   рамка
│ │ ┌─────── padding ──────┐ │ │   внутренний отступ
│ │ │      content         │ │ │   содержимое
│ │ └──────────────────────┘ │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘
\`\`\`

\`padding\` — внутри рамки, красится фоном элемента. \`margin\` — снаружи, всегда прозрачный.

## box-sizing

По умолчанию \`width\` задаёт ширину **только содержимого**. Реальная ширина получается больше:

\`\`\`css
.card { width: 300px; padding: 20px; border: 1px solid; }
/* занимает 300 + 20*2 + 1*2 = 342 px */
\`\`\`

Это неудобно. Поэтому первым правилом в файле стилей пишут:

\`\`\`css
* { box-sizing: border-box; }
\`\`\`

Теперь \`width: 300px\` — это итоговая ширина вместе с padding и border. Так работает и Bootstrap.

## Сокращённая запись

\`\`\`css
padding: 16px;                /* со всех сторон */
padding: 8px 16px;            /* сверху-снизу | слева-справа */
padding: 8px 16px 12px;       /* верх | бока | низ */
padding: 8px 16px 12px 4px;   /* верх, право, низ, лево — по часовой */
\`\`\`

Отдельные стороны: \`padding-top\`, \`margin-inline\` (слева и справа), \`margin-block\` (сверху и снизу).

\`margin-inline: auto\` центрирует блок с заданной шириной по горизонтали.

## Схлопывание margin

Вертикальные внешние отступы соседних блоков **не складываются**, а сливаются: если у одного \`margin-bottom: 20px\`, а у другого \`margin-top: 30px\`, расстояние будет 30 px, а не 50.

Это удивляет новичков, но задумано специально. Схлопывания не происходит внутри flex- и grid-контейнеров — ещё один довод пользоваться \`gap\` вместо margin при раскладке.

## display

| Значение | Поведение |
|---|---|
| \`block\` | Занимает всю ширину, начинается с новой строки (div, p, h1) |
| \`inline\` | Внутри строки, не принимает width/height (span, a) |
| \`inline-block\` | В строке, но размеры задавать можно |
| \`flex\` / \`grid\` | Контейнер раскладки |
| \`none\` | Элемент исчезает и не занимает места |

Для скрытия элемента есть разница: \`display: none\` убирает элемент из потока и из доступности, а \`visibility: hidden\` оставляет пустое место.`,
    examples: [
      {
        title: 'Карточка формы входа по центру',
        language: 'css',
        code: `* { box-sizing: border-box; }

.auth-card {
  width: 100%;
  max-width: 380px;
  margin: 48px auto;
  padding: 24px;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  background: #fff;
  box-shadow: 0 1px 3px rgb(0 0 0 / .08);
}

.auth-card input {
  width: 100%;
  padding: 10px 12px;
  margin-bottom: 12px;
  border: 1px solid #d1d5db;
  border-radius: 8px;
}`,
        explanation: 'Ровно такая карточка требуется в задании недели 2: форма входа в «карточке» по центру.',
      },
    ],
    mistakes: [
      {
        title: 'Не поставить box-sizing: border-box',
        why: 'Блоки с padding начинают вылезать за контейнер, и на 390 px появляется прокрутка.',
      },
      {
        title: 'Путать padding и margin',
        why: 'Фон заливает padding, но не margin. Если между рамкой и текстом нужен воздух — это padding.',
      },
      {
        title: 'Отступы через <br> и пустые div',
        why: 'Расстояния задаются margin, padding или gap. Пустые элементы ломают семантику.',
      },
    ],
    quizId: 'quiz-css-box-model',
    taskIds: ['task-css-card'],
    resources: [
      {
        title: 'MDN: блочная модель',
        url: 'https://developer.mozilla.org/ru/docs/Learn/CSS/Building_blocks/The_box_model',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-design', 'm2-mobile'],
    prerequisites: ['css-basics'],
    estimatedMinutes: 45,
    planDays: ['day-02-5'],
    source: 'plan',
  },

  {
    id: 'css-pseudo',
    title: 'Псевдоклассы: hover, focus, invalid, nth-child',
    tech: ['css'],
    monthNo: 1,
    weekNo: 2,
    importance: 'core',
    summary:
      'Псевдоклассы показывают состояние элемента: наведение, фокус, ошибка. Без видимого фокуса интерфейс недоступен с клавиатуры, а это часть оценки качества.',
    mustKnow: [
      ':hover, :active, :focus, :focus-visible',
      ':disabled, :checked',
      ':invalid, :valid и их подвох с пустым полем',
      ':nth-child(odd/even) для полосатых таблиц',
      ':not() и :first-child / :last-child',
    ],
    theory: `## Состояния кнопки

\`\`\`css
.btn {
  background: var(--brand);
  color: #fff;
  border: 0;
  padding: 10px 16px;
  border-radius: 8px;
  cursor: pointer;
  transition: background .18s ease;
}

.btn:hover   { background: #1d4ed8; }
.btn:active  { transform: translateY(1px); }
.btn:focus-visible { outline: 3px solid #93c5fd; outline-offset: 2px; }
.btn:disabled { background: #9ca3af; cursor: not-allowed; }
\`\`\`

**\`:focus-visible\` вместо \`:focus\`** — важная тонкость. \`:focus\` срабатывает и при клике мышью, из-за чего многие убирают обводку совсем (\`outline: none\`) — и ломают навигацию с клавиатуры. \`:focus-visible\` показывает обводку только тогда, когда пользователь пришёл с клавиатуры. Так и красиво, и доступно.

## Поля формы

\`\`\`css
input:focus { border-color: var(--brand); }
input:disabled { background: #f3f4f6; }
\`\`\`

С \`:invalid\` есть подвох: пустое обязательное поле считается невалидным **сразу** при загрузке страницы, и форма встречает пользователя красным. Решения:

\`\`\`css
/* подсвечивать только после ввода */
input:not(:placeholder-shown):invalid { border-color: #dc2626; }

/* или использовать свой класс, который ставит JS */
input.is-invalid { border-color: #dc2626; }
\`\`\`

Второй вариант — то, как делает Bootstrap (\`is-invalid\` + \`invalid-feedback\`), и именно он понадобится для требования «подсказки об ошибках рядом с формой».

## Структурные псевдоклассы

\`\`\`css
tbody tr:nth-child(even) { background: #f9fafb; }  /* полосы в таблице */
.list li:first-child { border-top: 0; }
.list li:last-child  { border-bottom: 0; }
.card:not(.card--muted) { border-color: #e5e7eb; }
\`\`\`

\`nth-child\` принимает \`odd\`, \`even\` или формулу \`3n\`. Полосатая таблица заявок читается заметно легче — дешёвый способ улучшить админку.

## Псевдоэлементы

Двоеточий два, а не одно:

\`\`\`css
.required::after { content: " *"; color: #dc2626; }
.card::before { content: ""; display: block; height: 4px; background: var(--brand); }
\`\`\`

Псевдоэлементы удобны для звёздочки у обязательного поля и для декоративных полосок — без лишней разметки.`,
    examples: [
      {
        title: 'Поле с ошибкой в стиле Bootstrap',
        language: 'css',
        code: `.form-field input {
  width: 100%;
  padding: 10px 12px;
  border: 1px solid #d1d5db;
  border-radius: 8px;
  transition: border-color .15s ease, box-shadow .15s ease;
}

.form-field input:focus-visible {
  outline: none;
  border-color: #2563eb;
  box-shadow: 0 0 0 3px rgb(37 99 235 / .2);
}

.form-field input.is-invalid { border-color: #dc2626; }

.form-field .invalid-feedback {
  display: none;
  color: #dc2626;
  font-size: .85rem;
  margin-top: 4px;
}

.form-field input.is-invalid ~ .invalid-feedback { display: block; }`,
        explanation:
          'Комбинация класса is-invalid и соседнего блока с текстом — готовый шаблон для требования «подсказки рядом с формой».',
      },
    ],
    mistakes: [
      {
        title: 'outline: none без замены',
        why: 'Пользователь с клавиатуры перестаёт понимать, где находится. Используйте :focus-visible со своей обводкой.',
      },
      {
        title: 'Красная форма при загрузке страницы',
        why: 'Из-за :invalid у пустых required-полей. Подсвечивайте ошибку после попытки отправки или после ввода.',
      },
      {
        title: 'Ставить hover-эффекты без обратной связи на касание',
        why: 'На телефоне нет наведения. Для 390×844 важнее :active и достаточный размер кнопки.',
      },
    ],
    quizId: 'quiz-css-pseudo',
    taskIds: ['task-css-form-states'],
    resources: [
      {
        title: 'MDN: псевдоклассы',
        url: 'https://developer.mozilla.org/ru/docs/Web/CSS/Pseudo-classes',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-register-hints', 'm2-design'],
    prerequisites: ['css-box-model'],
    estimatedMinutes: 40,
    planDays: ['day-02-6'],
    source: 'plan',
  },

  {
    id: 'css-flexbox',
    title: 'Flexbox: раскладка в одну линию',
    tech: ['css'],
    monthNo: 1,
    weekNo: 3,
    importance: 'core',
    summary:
      'Flexbox расставляет элементы в ряд или в колонку и распределяет между ними место. Это основной инструмент для шапки, карточек и кнопок.',
    mustKnow: [
      'display: flex и главная/поперечная ось',
      'justify-content, align-items, gap',
      'flex-direction: row | column',
      'flex-wrap: wrap для переноса на узком экране',
      'flex: 1 и flex-basis',
    ],
    theory: `## Две оси

\`\`\`css
.header { display: flex; }
\`\`\`

Как только элемент стал flex-контейнером, его прямые дети выстраиваются в ряд. У контейнера появляются две оси:

- **главная** — вдоль неё идут элементы (по умолчанию слева направо);
- **поперечная** — перпендикулярная ей.

\`justify-content\` управляет распределением по **главной** оси, \`align-items\` — по **поперечной**. Если поменять \`flex-direction\` на \`column\`, оси меняются местами — это самая частая путаница у новичков.

## Основные свойства контейнера

\`\`\`css
.header {
  display: flex;
  justify-content: space-between;  /* логотип слева, меню справа */
  align-items: center;             /* по вертикали по центру */
  gap: 16px;                       /* расстояние между элементами */
  flex-wrap: wrap;                 /* перенос, если не помещается */
}
\`\`\`

| \`justify-content\` | Результат |
|---|---|
| \`flex-start\` | Всё слева (по умолчанию) |
| \`center\` | По центру |
| \`flex-end\` | Всё справа |
| \`space-between\` | Края прижаты, промежутки равны |
| \`space-around\` | Равные поля вокруг каждого |
| \`space-evenly\` | Равные промежутки, включая края |

\`align-items\`: \`stretch\` (по умолчанию — растянуть), \`center\`, \`flex-start\`, \`flex-end\`, \`baseline\`.

**\`gap\`** — самый удобный способ задать расстояния: не нужно возиться с \`margin\` у последнего элемента.

## Свойства детей

\`\`\`css
.card { flex: 1; }          /* занять всё доступное место поровну */
.sidebar { flex: 0 0 260px; }/* не расти, не сжиматься, ширина 260px */
.logo { flex-shrink: 0; }    /* не сжиматься никогда */
\`\`\`

Запись \`flex: 1\` — это сокращение \`flex-grow: 1; flex-shrink: 1; flex-basis: 0\`. Три карточки с \`flex: 1\` получат одинаковую ширину независимо от содержимого.

## Центрирование

\`\`\`css
.center {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}
\`\`\`

Форма входа по центру экрана — четыре строки.

## Типичные раскладки проекта

\`\`\`css
/* Шапка: логотип слева, меню справа */
.navbar { display: flex; justify-content: space-between; align-items: center; }

/* Карточки помещений в ряд с переносом */
.rooms { display: flex; flex-wrap: wrap; gap: 16px; }
.rooms > * { flex: 1 1 260px; }   /* минимум 260px, дальше растут */

/* Кнопки формы */
.actions { display: flex; gap: 8px; justify-content: flex-end; }
\`\`\`

Строка \`flex: 1 1 260px\` — это «адаптив без медиазапросов»: на широком экране получится три карточки в ряд, на 390 px — одна под другой.`,
    examples: [
      {
        title: 'Шапка сайта',
        language: 'css',
        code: `.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 12px 20px;
  background: #fff;
  border-bottom: 1px solid #e5e7eb;
}

.navbar__menu {
  display: flex;
  gap: 20px;
  list-style: none;
  margin: 0;
  padding: 0;
}

@media (max-width: 600px) {
  .navbar__menu { display: none; }   /* уступает место бургеру */
}`,
        runnable: true,
      },
    ],
    mistakes: [
      {
        title: 'Забыть flex-wrap на узком экране',
        why: 'Без wrap элементы сжимаются до нечитаемого состояния или вылезают за границу контейнера.',
      },
      {
        title: 'Путать оси при flex-direction: column',
        why: 'В колонке justify-content управляет вертикалью, а align-items — горизонталью. Проверяйте в DevTools.',
      },
      {
        title: 'Отступы через margin вместо gap',
        why: 'gap не оставляет лишнего отступа у крайнего элемента и не схлопывается.',
      },
    ],
    quizId: 'quiz-css-flexbox',
    taskIds: ['task-css-navbar'],
    projectIds: ['project-01-event-page'],
    resources: [
      {
        title: 'MDN: Flexbox',
        url: 'https://developer.mozilla.org/ru/docs/Learn/CSS/CSS_layout/Flexbox',
        kind: 'docs',
        source: 'docs',
      },
      {
        title: 'Flexbox Froggy',
        url: 'https://flexboxfroggy.com/#ru',
        kind: 'practice',
        source: 'plan',
        note: 'Игра из учебной программы, день 3.1',
      },
    ],
    examRefs: ['m2-mobile', 'm2-design'],
    prerequisites: ['css-box-model'],
    estimatedMinutes: 50,
    planDays: ['day-03-1', 'day-03-2'],
    source: 'plan',
  },

  {
    id: 'css-grid',
    title: 'Grid: сетка из строк и колонок',
    tech: ['css'],
    monthNo: 1,
    weekNo: 3,
    importance: 'supporting',
    summary:
      'Grid раскладывает элементы сразу в двух измерениях. Для сетки карточек это одна строка кода, которая сама адаптируется под ширину экрана.',
    mustKnow: [
      'display: grid и grid-template-columns',
      'Единица fr и функция repeat()',
      'auto-fit / auto-fill вместе с minmax()',
      'gap (row-gap, column-gap)',
      'Когда Grid, а когда Flexbox',
    ],
    theory: `## Основа

\`\`\`css
.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
\`\`\`

\`1fr\` — «одна доля свободного места». Три колонки по \`1fr\` — три равные колонки. В отличие от процентов, \`fr\` учитывает \`gap\`, поэтому ничего не вылезает за край.

## Адаптивная сетка без медиазапросов

\`\`\`css
.rooms {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 16px;
}
\`\`\`

Читается так: «сделай столько колонок, сколько влезет, но каждая не уже 260 px и не шире одной доли».

- на ширине 1200 px → четыре колонки;
- на планшете → две;
- на 390 px → одна.

Ни одного \`@media\`. Для карточек помещений (аудитория, коворкинг, кинозал) это идеальный вариант.

Разница \`auto-fit\` и \`auto-fill\`: при малом числе элементов \`auto-fit\` растягивает их на всю ширину, \`auto-fill\` оставляет пустые колонки. Обычно нужен \`auto-fit\`.

## Явные строки и области

\`\`\`css
.layout {
  display: grid;
  grid-template-columns: 240px 1fr;
  grid-template-rows: auto 1fr auto;
  min-height: 100vh;
}
\`\`\`

Это каркас страницы с боковым меню: колонка фиксированной ширины и основная область.

## Размещение элемента

\`\`\`css
.wide { grid-column: span 2; }        /* занять две колонки */
.full { grid-column: 1 / -1; }        /* от первой до последней */
\`\`\`

## Grid или Flexbox

| Задача | Инструмент |
|---|---|
| Шапка: лого слева, меню справа | Flexbox |
| Ряд кнопок | Flexbox |
| Сетка карточек | Grid |
| Каркас страницы с сайдбаром | Grid |
| Элементы разной ширины в строку с переносом | Flexbox с wrap |

Проще запомнить: **одна линия — Flexbox, сетка — Grid**. Их спокойно используют вместе: grid для страницы, flex внутри карточки.`,
    examples: [
      {
        title: 'Карточки помещений',
        language: 'css',
        code: `.rooms {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 16px;
}

.room-card {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 16px;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  background: #fff;
}

.room-card img {
  width: 100%;
  height: 160px;
  object-fit: cover;
  border-radius: 8px;
}`,
        runnable: true,
      },
    ],
    mistakes: [
      {
        title: 'Проценты вместо fr',
        wrong: 'grid-template-columns: 33% 33% 33%; gap: 16px;',
        right: 'grid-template-columns: repeat(3, 1fr); gap: 16px;',
        why: 'Проценты не учитывают gap, и сетка выходит за контейнер.',
      },
      {
        title: 'Фиксированные колонки на мобильном',
        why: 'repeat(3, 1fr) на 390 px даёт три колонки по 110 px. Используйте auto-fit с minmax.',
      },
    ],
    quizId: 'quiz-css-grid',
    taskIds: ['task-css-grid-cards'],
    resources: [
      {
        title: 'MDN: CSS Grid',
        url: 'https://developer.mozilla.org/ru/docs/Learn/CSS/CSS_layout/Grids',
        kind: 'docs',
        source: 'docs',
      },
      { title: 'Grid Garden', url: 'https://cssgridgarden.com/#ru', kind: 'practice', source: 'plan' },
    ],
    examRefs: ['m2-mobile', 'm2-design'],
    prerequisites: ['css-flexbox'],
    estimatedMinutes: 45,
    planDays: ['day-03-3'],
    source: 'plan',
  },

  {
    id: 'css-position',
    title: 'Позиционирование: relative, absolute, fixed, sticky',
    tech: ['css'],
    monthNo: 1,
    weekNo: 3,
    importance: 'supporting',
    summary:
      'Позиционирование нужно точечно: закреплённая шапка, бейдж статуса в углу карточки, всплывающее уведомление и модальное окно.',
    mustKnow: [
      'static — по умолчанию, элемент в общем потоке',
      'relative — сдвиг относительно себя и точка отсчёта для absolute',
      'absolute — относительно ближайшего позиционированного предка',
      'fixed — относительно окна, не прокручивается',
      'sticky — прилипает при прокрутке; z-index',
    ],
    theory: `## Пять значений

**\`static\`** — как есть, свойства \`top/left\` не работают.

**\`relative\`** — элемент остаётся в потоке (место за ним сохраняется), но можно сдвинуть через \`top/left/right/bottom\`. Главное применение — сделать контейнер точкой отсчёта для \`absolute\`.

**\`absolute\`** — элемент выпадает из потока и позиционируется относительно ближайшего предка с \`position\`, отличным от \`static\`. Если такого нет — относительно страницы.

\`\`\`css
.card { position: relative; }
.card .badge { position: absolute; top: 8px; right: 8px; }
\`\`\`

Это бейдж статуса заявки в углу карточки — ровно то, что просит программа на неделе 3.

**\`fixed\`** — относительно окна браузера. Не двигается при прокрутке. Годится для всплывающего уведомления (toast) и модального окна.

**\`sticky\`** — гибрид: элемент ведёт себя обычно, пока не дойдёт до заданной границы, а потом «прилипает».

\`\`\`css
.navbar { position: sticky; top: 0; z-index: 10; background: #fff; }
\`\`\`

Sticky не работает, если у родителя \`overflow: hidden\` — самая частая причина «почему не прилипает».

## z-index

\`z-index\` работает только у позиционированных элементов (не \`static\`). Чем больше число, тем выше слой.

Договоритесь о шкале заранее, чтобы не писать \`z-index: 99999\`:

\`\`\`css
--z-header: 10;
--z-dropdown: 20;
--z-toast: 30;
--z-modal: 40;
\`\`\`

## Уведомление и модальное окно

\`\`\`css
.toast {
  position: fixed;
  right: 16px;
  bottom: 16px;
  z-index: 30;
}

.modal-backdrop {
  position: fixed;
  inset: 0;                    /* top/right/bottom/left = 0 */
  background: rgb(0 0 0 / .5);
  display: grid;
  place-items: center;
  z-index: 40;
}
\`\`\`

\`inset: 0\` — короткая запись для растягивания на весь экран, а \`place-items: center\` центрирует содержимое по обеим осям.

Оба элемента понадобятся в модуле 2: «всплывающие окна уведомлений» в админке и подтверждение смены статуса.`,
    examples: [
      {
        title: 'Карточка заявки с бейджем статуса',
        language: 'html',
        code: `<article class="app-card">
  <span class="app-card__badge">Новая</span>
  <h3>Коворкинг</h3>
  <p>14.09.2026 · Банковская карта</p>
</article>

<style>
.app-card { position: relative; padding: 16px; border: 1px solid #e5e7eb; border-radius: 12px; }
.app-card__badge {
  position: absolute; top: 12px; right: 12px;
  padding: 2px 10px; border-radius: 999px;
  background: #dbeafe; color: #1e40af; font-size: 12px;
}
</style>`,
        runnable: true,
      },
    ],
    mistakes: [
      {
        title: 'absolute без relative у родителя',
        why: 'Элемент улетает в угол страницы, а не карточки. Родителю нужен position: relative.',
      },
      {
        title: 'z-index у static-элемента',
        why: 'Свойство просто игнорируется. Сначала задайте position.',
      },
      {
        title: 'Строить всю раскладку на absolute',
        why: 'Такая вёрстка не адаптируется. Раскладка — flex и grid, позиционирование — точечно.',
      },
    ],
    quizId: 'quiz-css-position',
    taskIds: ['task-css-badge'],
    resources: [
      {
        title: 'MDN: position',
        url: 'https://developer.mozilla.org/ru/docs/Web/CSS/position',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-admin-tools', 'm2-design'],
    prerequisites: ['css-box-model'],
    estimatedMinutes: 40,
    planDays: ['day-03-4'],
    source: 'plan',
  },

  {
    id: 'css-responsive',
    title: 'Адаптив: медиазапросы и mobile-first, экран 390×844',
    tech: ['css'],
    monthNo: 1,
    weekNo: 3,
    importance: 'core',
    summary:
      'Задание требует совместимость с экраном 390×844. Правильный путь — писать стили сначала для узкого экрана, а потом расширять медиазапросами.',
    mustKnow: [
      'Без meta viewport адаптива не будет',
      '@media (min-width: …) и mobile-first',
      'Типичные точки: 576, 768, 992, 1200',
      'Как убрать горизонтальную прокрутку',
      'Проверка в DevTools на 390×844',
    ],
    theory: `## Две предпосылки

1. \`<meta name="viewport" content="width=device-width, initial-scale=1">\` в \`head\`.
2. Никаких фиксированных ширин больше 390 px у блоков.

Без первого пункта медиазапросы не сработают вообще.

## Mobile-first

Пишем базовые стили для узкого экрана, затем **добавляем** правила для широкого:

\`\`\`css
/* база: телефон */
.rooms { display: grid; gap: 12px; }

/* планшет и шире */
@media (min-width: 768px) {
  .rooms { grid-template-columns: repeat(2, 1fr); }
}

/* десктоп */
@media (min-width: 1200px) {
  .rooms { grid-template-columns: repeat(3, 1fr); }
}
\`\`\`

Почему именно так, а не наоборот (\`max-width\`): базовые стили получаются проще, а мобильная версия — та, которую проверяют на экзамене, — работает даже если медиазапросы не сработали.

## Точки перелома

Ориентиры Bootstrap 5: 576 (sm), 768 (md), 992 (lg), 1200 (xl). Держитесь их — так ваши стили и классы Bootstrap не будут конфликтовать.

Экран из задания — 390 px — попадает в базовый диапазон (меньше 576), то есть описывается стилями **без** медиазапросов.

## Как убрать горизонтальную прокрутку

Проверьте по списку:

\`\`\`css
* { box-sizing: border-box; }
img, video { max-width: 100%; height: auto; }
.container { width: min(100% - 2rem, 1100px); margin-inline: auto; }
table { width: 100%; }
.table-wrap { overflow-x: auto; }      /* таблицу — в скролл */
pre, code { overflow-x: auto; }
\`\`\`

Частые виновники: длинное слово без пробелов (лечится \`overflow-wrap: anywhere\`), картинка без \`max-width\`, блок с \`width\` в пикселях, отрицательный margin.

## Мобильные мелочи, которые замечают

- кнопка и поле — минимум 44 px по высоте, иначе в них трудно попасть пальцем;
- размер шрифта в полях — не меньше 16 px (иначе iOS увеличивает страницу при фокусе);
- меню превращается в бургер;
- таблица админки — в скролл или в карточки;
- модальное окно не должно быть шире экрана.

## Проверка

DevTools → Ctrl+Shift+M → Responsive → 390 × 844. Пройдите все страницы: вход, регистрация, кабинет, заявка, админка. Программа предлагает делать это в конце каждого дня вёрстки, а на неделе 20 — по чек-листу.`,
    examples: [
      {
        title: 'Таблица превращается в карточки',
        language: 'css',
        code: `@media (max-width: 575px) {
  .apps-table thead { display: none; }
  .apps-table tr {
    display: block;
    margin-bottom: 12px;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    padding: 12px;
  }
  .apps-table td { display: flex; justify-content: space-between; gap: 12px; border: 0; }
  .apps-table td::before { content: attr(data-label); color: #6b7280; }
}`,
        explanation:
          'В разметке у каждой ячейки ставится data-label="Помещение" и т.д. На узком экране подпись берётся из атрибута.',
      },
    ],
    mistakes: [
      {
        title: 'Проверять адаптив только сужением окна',
        why: 'Окно редко доходит до 390 px. Используйте режим устройства в DevTools.',
      },
      {
        title: 'Только max-width-запросы',
        why: 'Мобильная версия оказывается набором исключений и ломается чаще. Mobile-first проще и надёжнее.',
      },
      {
        title: 'Горизонтальная прокрутка из-за одной картинки',
        why: 'Правило img { max-width: 100% } должно стоять в базовых стилях всегда.',
      },
    ],
    quizId: 'quiz-css-responsive',
    taskIds: ['task-css-responsive-390', 'task-css-grid-cards'],
    projectIds: ['project-01-event-page'],
    resources: [
      {
        title: 'MDN: медиазапросы',
        url: 'https://developer.mozilla.org/ru/docs/Web/CSS/Media_Queries/Using_media_queries',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-mobile', 'm3-mobile'],
    prerequisites: ['css-flexbox', 'css-grid'],
    estimatedMinutes: 50,
    planDays: ['day-03-5'],
    source: 'plan',
  },

  {
    id: 'css-images',
    title: 'Изображения: object-fit, форматы и оптимизация',
    tech: ['css', 'design'],
    monthNo: 1,
    weekNo: 3,
    importance: 'core',
    summary:
      'Слайдер модуля 2 требует четыре изображения одинакового размера. Добиться этого проще всего через object-fit, а не переделкой картинок в редакторе.',
    mustKnow: [
      'max-width: 100% и height: auto — базовое правило',
      'object-fit: cover / contain и object-position',
      'aspect-ratio для одинаковых пропорций',
      'Форматы: JPG, PNG, WebP, SVG — что когда',
      'loading="lazy" и почему стоит указывать width/height',
    ],
    theory: `## Базовое правило

\`\`\`css
img { max-width: 100%; height: auto; display: block; }
\`\`\`

Картинка не вылезет за контейнер и сохранит пропорции. \`display: block\` убирает лишний отступ снизу, который появляется из-за строчного поведения.

## Одинаковый размер: object-fit

Задание требует «четыре одинаковых по размерам изображения». Если исходники разные, приводим их к одному размеру средствами CSS:

\`\`\`css
.slide img {
  width: 100%;
  height: 320px;
  object-fit: cover;       /* заполнить, лишнее обрезать */
  object-position: center;
}
\`\`\`

- \`cover\` — заполняет всю область, обрезая лишнее. Пропорции сохраняются, пустых полей нет. Для слайдера нужен именно он.
- \`contain\` — вписывает целиком, возможны пустые поля.
- \`fill\` — растягивает, искажая. Так делать не надо.

Альтернатива фиксированной высоте — одинаковые пропорции:

\`\`\`css
.slide img { width: 100%; aspect-ratio: 16 / 9; object-fit: cover; }
\`\`\`

Тогда высота считается от ширины, и на 390 px слайдер не займёт пол-экрана.

## Форматы

| Формат | Когда |
|---|---|
| **JPG** | Фотографии. Маленький размер, нет прозрачности |
| **PNG** | Прозрачность, скриншоты, чёткие границы |
| **WebP** | Современная замена JPG/PNG: меньше на 25–35 % |
| **SVG** | Иконки, логотипы. Векторный, масштабируется без потерь |

Для иконок на экзамене берите SVG — их можно вставить прямо в разметку и красить через \`fill: currentColor\`, не подключая шрифт иконок из интернета.

## Оптимизация

Модуль 2 прямо называет «оптимизацию изображений». Практически это значит:

1. Не класть в проект фото 4000×3000 px, если оно показывается в блоке 600 px. Уменьшите заранее.
2. Пережать: JPG с качеством 75–85 визуально неотличим, а весит в разы меньше.
3. Указать \`width\` и \`height\` в разметке — браузер зарезервирует место, и страница не «прыгнет».
4. Добавить \`loading="lazy"\` тем картинкам, которые не видны сразу. Первому слайду \`lazy\` **не** ставят — он виден сразу.

\`\`\`html
<img src="img/hall-1.jpg" alt="Аудитория" width="800" height="450" loading="lazy">
\`\`\`

## Фон против img

Если изображение — часть содержания (фото помещения), это \`<img>\` с \`alt\`. Если это декор (градиент, узор), это \`background-image\` в CSS.`,
    examples: [
      {
        title: 'Четыре слайда одинакового размера',
        language: 'html',
        code: `<div class="slider">
  <div class="slider__track">
    <img src="img/1.jpg" alt="Аудитория на 120 мест">
    <img src="img/2.jpg" alt="Коворкинг" loading="lazy">
    <img src="img/3.jpg" alt="Кинозал" loading="lazy">
    <img src="img/4.jpg" alt="Холл для регистрации" loading="lazy">
  </div>
</div>

<style>
.slider { overflow: hidden; border-radius: 12px; }
.slider__track { display: flex; transition: transform .4s ease; }
.slider img {
  flex: 0 0 100%;
  width: 100%;
  aspect-ratio: 16 / 9;
  object-fit: cover;
}
</style>`,
        explanation:
          'Разные по размеру исходники выглядят одинаково: aspect-ratio задаёт пропорции, object-fit: cover обрезает лишнее.',
        runnable: true,
      },
    ],
    mistakes: [
      {
        title: 'Растягивать картинки заданием width и height без object-fit',
        why: 'Изображения искажаются. Требование «одинаковые по размерам» выполнено формально, а выглядит плохо.',
      },
      {
        title: 'Класть в проект тяжёлые исходники',
        why: 'Модуль 2 прямо оценивает оптимизацию изображений. Четыре фото по 5 МБ — это минус.',
      },
      {
        title: 'loading="lazy" на первом экране',
        why: 'Первое изображение должно грузиться сразу, иначе пользователь видит пустоту.',
      },
    ],
    quizId: 'quiz-css-images',
    taskIds: ['task-css-object-fit'],
    resources: [
      {
        title: 'MDN: object-fit',
        url: 'https://developer.mozilla.org/ru/docs/Web/CSS/object-fit',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m2-slider', 'm2-design'],
    prerequisites: ['css-box-model'],
    estimatedMinutes: 40,
    planDays: ['day-03-6'],
    source: 'plan',
  },

  {
    id: 'css-animations',
    title: 'Микроанимации: transition, transform, @keyframes',
    tech: ['css'],
    monthNo: 1,
    weekNo: 4,
    importance: 'core',
    summary:
      'Модуль 3 рекомендует микроанимации. Правило простое: коротко (150–300 мс), по делу и с уважением к настройке «уменьшить движение».',
    mustKnow: [
      'transition: свойство длительность плавность',
      'transform: scale, translate, rotate — анимируется дёшево',
      '@keyframes и animation',
      'Анимировать transform и opacity, а не width и top',
      'prefers-reduced-motion',
    ],
    theory: `## transition

\`\`\`css
.btn {
  background: #2563eb;
  transition: background .2s ease, transform .15s ease;
}
.btn:hover { background: #1d4ed8; }
.btn:active { transform: translateY(1px); }
\`\`\`

Запись читается так: свойство, длительность, кривая. Можно перечислить несколько через запятую.

\`transition: all .3s\` работает, но анимирует лишнее и иногда тормозит — лучше перечислять свойства явно.

**Длительность.** 150–250 мс для мелочей (кнопка, поле), 250–350 мс для появления блоков. Дольше 400 мс интерфейс кажется медленным.

## transform

\`\`\`css
transform: translateY(-2px);
transform: scale(1.03);
transform: rotate(180deg);
transform: translateX(-100%);   /* сдвиг слайдера */
\`\`\`

Браузер выполняет \`transform\` и \`opacity\` на видеокарте, не пересчитывая раскладку. Поэтому они анимируются плавно, а \`width\`, \`height\`, \`top\`, \`margin\` — дёргаются. Правило: **анимируем transform и opacity**.

Сдвиг ленты слайдера — как раз \`transform: translateX(-100% * индекс)\`.

## @keyframes

Когда нужно не «из состояния в состояние», а последовательность:

\`\`\`css
@keyframes toast-in {
  from { opacity: 0; transform: translateY(-12px); }
  to   { opacity: 1; transform: translateY(0); }
}

.toast { animation: toast-in .25s ease-out; }
\`\`\`

Параметры \`animation\`: имя, длительность, кривая, задержка, число повторов (\`infinite\`), направление (\`alternate\`), заполнение (\`forwards\` — остаться в конечном состоянии).

Спиннер загрузки:

\`\`\`css
@keyframes spin { to { transform: rotate(360deg); } }
.spinner { animation: spin 1s linear infinite; }
\`\`\`

## prefers-reduced-motion

Некоторым людям анимации вызывают головокружение, и они включают в системе «уменьшить движение». Уважить эту настройку — две строки:

\`\`\`css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: .01ms !important;
  }
}
\`\`\`

Это и требование доступности, и заметный плюс к «качеству кода».

## Сколько анимаций нужно

Программа советует пять и не больше:

1. кнопки и ссылки при наведении;
2. поле при фокусе;
3. появление карточек или списка;
4. появление уведомления;
5. переключение слайда.

Больше — начинает мешать.`,
    examples: [
      {
        title: 'Набор микроанимаций проекта',
        language: 'css',
        code: `.btn, .card, .form-control {
  transition: transform .15s ease, box-shadow .2s ease, border-color .15s ease;
}

.card:hover { transform: translateY(-2px); box-shadow: 0 6px 16px rgb(0 0 0 / .08); }
.form-control:focus { border-color: #2563eb; box-shadow: 0 0 0 3px rgb(37 99 235 / .18); }

@keyframes fade-up {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: none; }
}
.list-item { animation: fade-up .25s ease-out both; }

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
  }
}`,
      },
    ],
    mistakes: [
      {
        title: 'Анимировать width, height, top',
        why: 'Браузер пересчитывает раскладку каждый кадр — анимация дёргается. Используйте transform.',
      },
      {
        title: 'Длинные анимации',
        why: 'Полсекунды на наведение кнопки ощущаются как тормоза.',
      },
      {
        title: 'Игнорировать prefers-reduced-motion',
        why: 'Это стандартное требование доступности, которое проверяют в критерии «качество кода».',
      },
    ],
    quizId: 'quiz-css-animations',
    taskIds: ['task-css-transition'],
    resources: [
      {
        title: 'MDN: transition',
        url: 'https://developer.mozilla.org/ru/docs/Web/CSS/CSS_Transitions/Using_CSS_transitions',
        kind: 'docs',
        source: 'docs',
      },
    ],
    examRefs: ['m3-animations', 'm2-design'],
    prerequisites: ['css-pseudo'],
    estimatedMinutes: 45,
    planDays: ['day-04-1', 'day-04-2'],
    source: 'plan',
  },

  {
    id: 'git-branches',
    title: 'Git: ветки, слияние и откат',
    tech: ['git'],
    monthNo: 1,
    weekNo: 4,
    importance: 'supporting',
    summary:
      'Ветка — отдельная линия работы. На экзамене это способ безопасно попробовать рискованное изменение и вернуться, если не получилось.',
    mustKnow: [
      'git branch, git switch -c, git merge',
      'Что такое HEAD и как понять, где вы сейчас',
      'Конфликт слияния и как его разрешить',
      'git log --oneline --graph',
      'git restore и git switch -',
    ],
    theory: `## Зачем ветки на экзамене

Основной сценарий такой: у вас работает модуль 1, начинается модуль 2 — переделка дизайна. Если что-то пойдёт не так, хочется мгновенно вернуться к рабочему состоянию.

\`\`\`bash
git switch -c design      # создать ветку и перейти в неё
# ... правки дизайна, коммиты ...
git switch main           # вернуться к рабочей версии
git merge design          # влить дизайн в основную ветку
\`\`\`

Программа на неделе 19 предлагает ещё один приём: сохранить готовый проект в отдельной ветке и написать модуль 1 заново с нуля, чтобы потренировать скорость.

## Основные команды

\`\`\`bash
git branch                # список веток
git switch -c feature-x   # создать и перейти
git switch main           # перейти в существующую
git switch -              # вернуться в предыдущую
git merge feature-x       # влить ветку в текущую
git branch -d feature-x   # удалить слитую ветку
\`\`\`

Старый вариант \`git checkout -b\` делает то же самое; \`switch\` появился позже и понятнее.

## Конфликт

Если один и тот же участок файла изменён в двух ветках, Git не решает за вас:

\`\`\`text
<<<<<<< HEAD
<button class="btn btn-primary">Отправить</button>
=======
<button class="btn btn-success">Отправить заявку</button>
>>>>>>> design
\`\`\`

Порядок действий: открыть файл, оставить нужный вариант, **удалить строки с маркерами**, затем \`git add файл\` и \`git commit\`. VS Code показывает конфликт с кнопками «Accept Current / Accept Incoming».

## История

\`\`\`bash
git log --oneline --graph --all
\`\`\`

Рисует ветвление текстом — удобно, когда запутались.

## Откат без паники

| Ситуация | Команда |
|---|---|
| Испортил файл, не коммитил | \`git restore файл\` |
| Добавил в индекс лишнее | \`git restore --staged файл\` |
| Хочу посмотреть, что изменилось | \`git diff\` |
| Ошибся в тексте последнего коммита | \`git commit --amend -m "Новый текст"\` |

\`git reset --hard\` удаляет незакоммиченные изменения безвозвратно — на экзамене к нему лучше не притрагиваться.

## Замечание про экзамен

Ветки — не требование задания: там сказано только про репозиторий и три коммита. Но ветка \`design\` перед модулем 2 — дешёвая страховка.`,
    examples: [
      {
        title: 'Работа над модулем 2 в отдельной ветке',
        language: 'bash',
        code: `git switch -c design
# ... стили, слайдер, адаптив ...
git add .
git commit -m "Дизайн: слайдер и адаптив 390px"
git switch main
git merge design
git log --oneline --graph`,
      },
    ],
    mistakes: [
      {
        title: 'Оставить маркеры конфликта в коде',
        why: 'Файл со строками <<<<<<< не запустится. Проверяйте поиском по проекту после слияния.',
      },
      {
        title: 'Коммитить в другой ветке по невнимательности',
        why: 'Всегда смотрите на строку состояния VS Code или git status — там написана текущая ветка.',
      },
    ],
    quizId: 'quiz-git-branches',
    taskIds: [],
    resources: [
      {
        title: 'learngitbranching.js.org',
        url: 'https://learngitbranching.js.org/?locale=ru_RU',
        kind: 'practice',
        source: 'plan',
      },
    ],
    examRefs: ['m1-git', 'm2-git'],
    prerequisites: ['git-basics'],
    estimatedMinutes: 40,
    planDays: ['day-04-3'],
    source: 'plan',
  },

  {
    id: 'design-basics',
    title: 'Основы интерфейсного дизайна: сетка, отступы, цвет, иконки',
    tech: ['design', 'css'],
    monthNo: 1,
    weekNo: 4,
    importance: 'core',
    summary:
      'Модуль 2 оценивает внешний вид. Аккуратность важнее украшений: единая шкала отступов, два-три цвета, читаемый текст и понятные состояния.',
    mustKnow: [
      'Шкала отступов, кратная 4 или 8',
      'Ограниченная палитра: основной, нейтральный, цвета состояний',
      'Иерархия: размер, вес, цвет — но не всё сразу',
      'Иконки одного набора и одного размера',
      'Состояния: обычное, наведение, фокус, отключено, загрузка, ошибка',
    ],
    theory: `## Шкала отступов

Выберите шаг — 4 или 8 пикселей — и пользуйтесь только кратными значениями: 4, 8, 12, 16, 24, 32, 48. Интерфейс сразу выглядит собранным, потому что расстояния повторяются, а не берутся «на глаз».

\`\`\`css
:root {
  --space-1: 4px;  --space-2: 8px;  --space-3: 12px;
  --space-4: 16px; --space-6: 24px; --space-8: 32px;
}
\`\`\`

Практическое правило: **отступ между связанными элементами меньше, чем между группами**. Подпись и поле — 4 px, между полями — 16 px, между блоками формы — 24 px. Тогда форма читается как набор групп, а не как список.

## Цвет

Достаточно четырёх ролей:

| Роль | Пример | Где |
|---|---|---|
| Основной | синий | Главная кнопка, ссылки, активный пункт |
| Нейтральный | серые | Текст, рамки, фон |
| Успех | зелёный | Статус «Мероприятие завершено», уведомление об успехе |
| Опасность | красный | Ошибка валидации |

Для трёх статусов заявки цвета подбираются осмысленно: «Новая» — нейтральный или синий, «Мероприятие назначено» — синий или жёлтый, «Мероприятие завершено» — зелёный. Главное — чтобы статус читался и текстом, а не только цветом: дальтоники не различат зелёный и красный бейдж.

**Контраст.** Обычный текст — не менее 4.5:1 к фону, крупный — 3:1. Светло-серый текст на белом выглядит «дизайнерски», но не читается.

## Иерархия

На странице должно быть понятно, что главное. Инструменты: размер, насыщенность, цвет, расположение. Использовать все сразу не надо — обычно хватает двух.

\`\`\`text
Заголовок страницы   24px / 600
Заголовок карточки   18px / 600
Основной текст       16px / 400
Подпись, мета        14px / 400, серый
\`\`\`

## Кнопки

Одна главная кнопка на экран — залитая цветом. Остальные — с рамкой или вовсе текстовые. Если на странице три ярких кнопки, пользователь не понимает, чего от него хотят.

## Иконки

Один набор, один размер (обычно 16, 20 или 24 px), одинаковая толщина линий. Иконка без подписи допустима только у общепонятных действий (крестик, поиск); в остальных случаях добавьте текст или хотя бы \`aria-label\`.

На экзамене интернета нет, поэтому иконки берут либо из локального набора (SVG-файлы), либо из Bootstrap Icons, установленных пакетом.

## Состояния

Для каждого интерактивного элемента продумайте шесть состояний: обычное, наведение, фокус с клавиатуры, нажатие, отключено, загрузка. Для страниц — три: загрузка, пусто, ошибка. Модуль 3 и раздел «качество кода» смотрят именно на это.

## Чего не делать

- Тени и градиенты «для красоты» на каждом блоке.
- Больше двух шрифтов.
- Анимации дольше 300 мс.
- Тонкий светло-серый текст.
- Разные скругления у соседних элементов.`,
    examples: [
      {
        title: 'Токены темы проекта',
        language: 'css',
        code: `:root {
  /* цвет */
  --brand: #2563eb;
  --brand-dark: #1d4ed8;
  --text: #111827;
  --muted: #6b7280;
  --line: #e5e7eb;
  --bg: #f9fafb;
  --success: #16a34a;
  --danger: #dc2626;
  --warning: #d97706;

  /* отступы */
  --space-2: 8px; --space-3: 12px; --space-4: 16px; --space-6: 24px;

  /* форма */
  --radius: 10px;
  --shadow: 0 1px 3px rgb(0 0 0 / .08);
}

.badge--new       { background: #e0e7ff; color: #3730a3; }
.badge--scheduled { background: #fef3c7; color: #92400e; }
.badge--finished  { background: #dcfce7; color: #166534; }`,
        explanation:
          'Три бейджа — это три статуса заявки из задания. Цвет плюс текст: понятно и без различения цветов.',
      },
    ],
    mistakes: [
      {
        title: 'Отступы «на глаз»',
        why: 'Разнобой в 3, 7, 11 px читается как неаккуратность даже теми, кто не занимается дизайном.',
      },
      {
        title: 'Статус только цветом',
        why: 'Пользователь с нарушением цветовосприятия не отличит «назначено» от «завершено». Пишите текст.',
      },
      {
        title: 'Пять ярких кнопок на экране',
        why: 'Пропадает иерархия. Главное действие должно быть одно.',
      },
    ],
    quizId: 'quiz-design-basics',
    taskIds: [],
    resources: [
      {
        title: 'WCAG: контраст текста',
        url: 'https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html',
        kind: 'reference',
        source: 'docs',
      },
    ],
    examRefs: ['m2-design', 'm2-admin-tools'],
    prerequisites: ['css-typography'],
    estimatedMinutes: 40,
    planDays: ['day-04-4'],
    source: 'author',
  },
];
