import type { Topic } from '../types';

/** Месяц 5, недели 17–20: сборка модулей 1 и 2 целиком. */
export const MONTH_05_TOPICS: Topic[] = [
  {
    id: 'project-structure',
    title: 'Каркас проекта: client, server, database',
    tech: ['tools', 'git'],
    monthNo: 5,
    weekNo: 17,
    importance: 'core',
    summary:
      'Первые 10 минут экзамена — это каркас: три папки, репозиторий, .gitignore и два запущенных терминала. Довести до автоматизма.',
    mustKnow: [
      'Структура папок из учебной программы',
      'Порядок действий первых десяти минут',
      '.gitignore до первого коммита',
      'Два терминала: клиент и сервер',
      'Первый коммит сразу после каркаса',
    ],
    theory: `## Структура

\`\`\`text
conference/
├── client/            React + TypeScript (Vite)
│   ├── public/img/    4 картинки для слайдера
│   ├── src/
│   │   ├── api/       запросы к серверу
│   │   ├── components/
│   │   ├── context/
│   │   ├── pages/
│   │   └── types.ts
│   └── vite.config.ts прокси /api → сервер
├── server/            Node.js + Express
│   ├── routes/
│   ├── middleware/
│   ├── repositories/
│   ├── db.js
│   ├── seed.js
│   └── index.js
├── database/
│   ├── schema.sql
│   ├── seed.sql
│   └── er-diagram.png
├── .gitignore
└── README.md
\`\`\`

## Первые десять минут экзамена

\`\`\`bash
mkdir conference && cd conference
git init
printf "node_modules/\\n.env\\ndist/\\n" > .gitignore
git add .gitignore && git commit -m "Старт проекта"

npm create vite@latest client -- --template react-ts
cd client && npm install && npm install bootstrap react-router-dom && cd ..

mkdir server && cd server
npm init -y
npm install express mysql2 bcryptjs jsonwebtoken
cd ..

git add . && git commit -m "Каркас: клиент и сервер"
\`\`\`

Если пакеты ставятся из кеша, добавьте \`--offline\`. Это нужно проверить **до** экзамена.

## Порядок работы

Учебная программа предлагает такую последовательность на модуль 1:

\`\`\`text
1. Каркас + репозиторий          10 мин   → коммит
2. schema.sql + ER-диаграмма     15 мин   → коммит
3. Сервер: подключение к БД,
   регистрация и вход            20 мин   → коммит
4. Клиент: регистрация и вход    20 мин   → коммит
5. Заявка и кабинет              15 мин   → коммит
6. Админка                       10 мин   → коммит
\`\`\`

Шесть коммитов вместо требуемых трёх — и на каждом этапе есть рабочая версия, к которой можно откатиться.

## Два терминала

\`\`\`bash
# терминал 1
cd server && node --watch index.js

# терминал 2
cd client && npm run dev
\`\`\`

В VS Code второй терминал открывается кнопкой «+» или \`Ctrl+Shift+\\\`\`. Потренируйтесь заранее: на экзамене это должно быть привычным действием.

## .gitignore до первого коммита

Если закоммитить \`node_modules\`, репозиторий раздуется до сотен мегабайт, а убрать папку из истории потом сложно. Файл \`.gitignore\` — самая первая команда после \`git init\`.

## README

\`\`\`markdown
# Конференции.РФ

## Запуск
1. Создать базу: mysql -u root -p < database/schema.sql
2. Сервер: cd server && npm install && npm run dev
3. Клиент: cd client && npm install && npm run dev

## Администратор
Логин: Admin26, пароль: Demo20

## API
(таблица маршрутов)
\`\`\`

README пишется в конце, но 10 минут на него стоит заложить: он показывает проверяющему, что проект законченный.`,
    examples: [
      {
        title: 'Скрипт создания каркаса',
        language: 'bash',
        code: `# Всё, что нужно в первые 10 минут
mkdir conference && cd conference
git init
printf "node_modules/\\n.env\\ndist/\\n" > .gitignore
git add .gitignore
git commit -m "Старт проекта: .gitignore"

# Клиент
npm create vite@latest client -- --template react-ts
cd client
npm install
npm install bootstrap react-router-dom
cd ..

# Сервер
mkdir server && cd server
npm init -y
npm pkg set type=module
npm pkg set scripts.dev="node --watch index.js"
npm install express mysql2 bcryptjs jsonwebtoken
cd ..

mkdir database

git add .
git commit -m "Каркас проекта: client, server, database"`,
      },
    ],
    mistakes: [
      {
        title: 'Начинать с интерфейса',
        why: 'Без базы и сервера красивые страницы нечем наполнить. Порядок: БД → сервер → клиент.',
      },
      {
        title: '.gitignore после первого коммита',
        why: 'node_modules попадёт в историю. Убирать оттуда долго и нервно.',
      },
      {
        title: 'Один коммит в конце',
        why: 'Задание требует минимум три промежуточных коммита в каждом модуле.',
      },
    ],
    quizId: 'quiz-project-structure',
    taskIds: [],
    resources: [],
    examRefs: ['m1-git', 'm3-framework'],
    prerequisites: ['react-structure', 'server-architecture'],
    estimatedMinutes: 45,
    planDays: ['day-17-1'],
    source: 'plan',
  },

  {
    id: 'fullstack-register',
    title: 'Регистрация целиком: форма → сервер → база',
    tech: ['react', 'express', 'sql', 'security'],
    monthNo: 5,
    weekNo: 17,
    importance: 'core',
    summary:
      'Первая сквозная цепочка: пользователь заполняет форму, данные проходят две проверки и попадают в базу с хешированным паролем.',
    mustKnow: [
      'Путь данных от формы до таблицы',
      'Две линии проверки: клиент и сервер',
      'Ответ 409 при занятом логине',
      'Показ серверной ошибки у поля',
      'Что проверить перед переходом к следующему пункту',
    ],
    theory: `## Путь данных

\`\`\`text
Форма React
  ↓ проверка на клиенте (быстрая подсказка)
fetch POST /api/register
  ↓ проверка на сервере (настоящая защита)
  ↓ проверка уникальности логина
  ↓ bcrypt.hash
INSERT INTO users
  ↓
201 + данные пользователя (без хеша)
  ↓
Переход на страницу входа
\`\`\`

## Две проверки — не дублирование

Клиентская даёт мгновенную подсказку и экономит запрос. Серверная — единственная настоящая защита: запрос можно отправить в обход формы.

Правила одни и те же, поэтому функцию валидации стоит написать так, чтобы её можно было скопировать на сервер почти без изменений.

## Ошибка «логин занят»

Эту ошибку клиент проверить не может. Сервер возвращает 409, клиент показывает её у поля логина:

\`\`\`tsx
try {
  await api.post('/register', values);
  navigate('/login');
} catch (error) {
  if (error.status === 409) {
    setErrors({ login: 'Такой логин уже занят' });
  } else {
    setErrors({ form: 'Не удалось зарегистрироваться. Попробуйте ещё раз.' });
  }
}
\`\`\`

## Чек-лист готовности пункта

- [ ] Все пять полей обязательны
- [ ] Логин: латиница и цифры, минимум 6 символов
- [ ] Пароль: минимум 8 символов
- [ ] Ошибки показываются рядом с полями
- [ ] Занятый логин даёт понятное сообщение
- [ ] Пароль в базе — хеш, а не открытый текст
- [ ] После успеха переход на вход
- [ ] Ссылка «Уже зарегистрированы? Вход» на месте
- [ ] Коммит

## Типичная потеря времени

Не пытайтесь сразу сделать красиво. Сначала цепочка должна заработать: форма → сервер → база. Внешний вид — это модуль 2, у него отдельные полтора часа.`,
    examples: [
      {
        title: 'Проверка цепочки вручную',
        language: 'text',
        code: `1. Открыть /register, отправить пустую форму
   → пять подсказок у полей, запроса к серверу нет

2. Логин «иванов26» (кириллица)
   → подсказка у поля логина

3. Корректные данные
   → переход на /login

4. В MySQL: SELECT login, password_hash FROM users ORDER BY id DESC LIMIT 1;
   → логин на месте, в password_hash строка вида $2a$10$...

5. Повторить регистрацию с тем же логином
   → «Такой логин уже занят» у поля логина

6. В консоли браузера отправить запрос напрямую:
   fetch('/api/register', { method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({ login: 'ab', password: '1' }) })
   → ответ 400, в базе ничего не появилось`,
        explanation:
          'Последний пункт — самая важная проверка: он подтверждает, что серверная валидация действительно работает, а не только форма.',
      },
    ],
    mistakes: [
      {
        title: 'Валидация только на клиенте',
        why: 'Проверяющий может отправить запрос напрямую — и увидит в базе пользователя с логином из одного символа.',
      },
      {
        title: 'Общее сообщение вместо ошибки у поля',
        why: 'Модуль 2 требует подсказки рядом с формой. «Проверьте данные» не объясняет, что исправить.',
      },
    ],
    quizId: 'quiz-fullstack-register',
    taskIds: ['task-fs-register-flow'],
    projectIds: ['project-05-module-one-run'],
    resources: [],
    examRefs: ['m1-register', 'm2-register-hints'],
    prerequisites: ['auth-hashing', 'react-forms'],
    estimatedMinutes: 50,
    planDays: ['day-17-2'],
    source: 'plan',
  },

  {
    id: 'fullstack-auth-flow',
    title: 'Вход, токен и меню по ролям',
    tech: ['react', 'express', 'security'],
    monthNo: 5,
    weekNo: 17,
    importance: 'core',
    summary:
      'После входа приложение меняется: появляются защищённые страницы, меню зависит от роли, а каждый запрос несёт токен.',
    mustKnow: [
      'Цепочка: вход → токен → localStorage → контекст',
      'Токен в каждом запросе',
      'Меню зависит от роли',
      'Выход очищает всё',
      'Точный текст ссылки из задания',
    ],
    theory: `## Цепочка

\`\`\`text
Форма входа
  ↓ POST /api/login
Сервер: найти пользователя → bcrypt.compare → jwt.sign
  ↓ { token, user }
localStorage + AuthContext
  ↓
Меню перерисовывается, открываются защищённые страницы
  ↓
Каждый запрос: Authorization: Bearer <токен>
\`\`\`

## Ссылки из задания

На странице входа — **дословно**:

\`\`\`tsx
<Link to="/register">Еще не зарегистрированы? Регистрация</Link>
\`\`\`

На странице регистрации — обратная ссылка (текст задание не фиксирует):

\`\`\`tsx
<Link to="/login">Уже зарегистрированы? Вход</Link>
\`\`\`

Первую проверяют буква в букву. «Ещё» с буквой «ё» — уже несовпадение.

## Меню по ролям

\`\`\`tsx
{user ? (
  <>
    <NavLink to="/order">Заявка</NavLink>
    <NavLink to="/cabinet">Кабинет</NavLink>
    {user.role === 'admin' && <NavLink to="/admin">Админка</NavLink>}
    <button onClick={handleLogout}>Выход</button>
  </>
) : (
  <NavLink to="/login">Вход</NavLink>
)}
\`\`\`

## Выход

\`\`\`tsx
const handleLogout = () => {
  logout();                              // чистит localStorage и контекст
  navigate('/login', { replace: true }); // replace, чтобы «назад» не вернул на закрытую страницу
};
\`\`\`

## Проверка администратора

Задание требует: админка защищена логином \`Admin26\` и паролем \`Demo20\`. Эти данные создаются seed-скриптом. Проверьте вход под ними отдельно — это один из самых очевидных пунктов для проверяющего.

## Чек-лист

- [ ] Вход по паре логин-пароль работает
- [ ] Неверные данные дают понятное сообщение
- [ ] Ссылка с точным текстом из задания
- [ ] Обратная ссылка на странице регистрации
- [ ] Токен сохраняется и подставляется в запросы
- [ ] После F5 пользователь остаётся в системе
- [ ] Меню зависит от роли
- [ ] Вход под Admin26 / Demo20 открывает админку
- [ ] Выход очищает данные
- [ ] Коммит`,
    examples: [
      {
        title: 'Проверка сохранения сессии',
        language: 'text',
        code: `1. Войти под обычным пользователем
2. Нажать F5
   → пользователь остался в системе, меню не сбросилось
3. DevTools → Application → Local Storage
   → есть ключи token и user
4. Открыть /admin вручную в адресной строке
   → перенаправление в кабинет
5. Выйти, снова открыть /cabinet
   → перенаправление на /login
6. Войти под Admin26 / Demo20
   → в меню появился пункт «Админка»`,
      },
    ],
    mistakes: [
      {
        title: 'Текст ссылки «по смыслу»',
        why: 'Задание задаёт формулировку дословно. Это самый дешёвый балл — и самый обидный, если потерять.',
      },
      {
        title: 'Сессия теряется после F5',
        why: 'Токен лежит в localStorage, но контекст его не восстанавливает при загрузке.',
      },
      {
        title: 'Проверка роли только в меню',
        why: 'Адрес /admin вводится руками. Нужны защищённый маршрут и проверка на сервере.',
      },
    ],
    quizId: 'quiz-fullstack-auth',
    taskIds: ['task-fs-menu-roles'],
    projectIds: ['project-05-module-one-run'],
    resources: [],
    examRefs: ['m1-login', 'm1-admin'],
    prerequisites: ['auth-jwt', 'react-context'],
    estimatedMinutes: 50,
    planDays: ['day-17-3'],
    source: 'plan',
  },

  {
    id: 'fullstack-order',
    title: 'Заявка: справочники, дата и статус по умолчанию',
    tech: ['react', 'express', 'sql'],
    monthNo: 5,
    weekNo: 17,
    importance: 'core',
    summary:
      'Страница оформления заявки собирает почти все требования сразу: выпадающие списки из базы, дата ДД.ММ.ГГГГ, запись со статусом «Новая».',
    mustKnow: [
      'Справочники загружаются с сервера',
      'Перевод даты между форматами',
      'Статус по умолчанию',
      'Пользователь берётся из токена',
      'Уведомление и переход после отправки',
    ],
    theory: `## Справочники

Выпадающие списки заполняются данными из базы, а не захардкожены:

\`\`\`tsx
const [rooms, setRooms] = useState<Room[]>([]);
const [payments, setPayments] = useState<PaymentMethod[]>([]);

useEffect(() => {
  Promise.all([api.get<Room[]>('/rooms'), api.get<PaymentMethod[]>('/payment-methods')])
    .then(([roomsData, paymentsData]) => {
      setRooms(roomsData);
      setPayments(paymentsData);
    })
    .catch(() => setError('Не удалось загрузить справочники'));
}, []);
\`\`\`

\`Promise.all\` грузит оба справочника одновременно — страница открывается вдвое быстрее.

## Дата

На экране — ДД.ММ.ГГГГ, на сервер уходит ГГГГ-ММ-ДД:

\`\`\`tsx
const iso = fromRuDate(values.date);
if (!iso) {
  setErrors({ date: 'Введите дату в формате ДД.ММ.ГГГГ' });
  return;
}
await api.post('/applications', { roomId: values.roomId, paymentId: values.paymentId, date: iso });
\`\`\`

## Статус и пользователь

Клиент **не передаёт** ни статус, ни идентификатор пользователя:

- статус проставляет база значением по умолчанию;
- пользователя сервер берёт из токена.

Если принимать \`userId\` из тела запроса, любой сможет создать заявку от чужого имени.

## После отправки

\`\`\`tsx
toast('Заявка отправлена');
navigate('/cabinet');
\`\`\`

Пользователь сразу видит свою новую заявку в списке — это лучшее подтверждение, что всё получилось.

## Чек-лист

- [ ] Помещение выбирается из выпадающего списка (данные из БД)
- [ ] Способ оплаты выбирается из выпадающего списка
- [ ] Дата вводится и показывается как ДД.ММ.ГГГГ
- [ ] Некорректная дата отклоняется с подсказкой
- [ ] Дата в прошлом не принимается
- [ ] Новая заявка получает статус «Новая»
- [ ] Заявка привязана к вошедшему пользователю
- [ ] После отправки — уведомление и переход в кабинет
- [ ] Коммит`,
    examples: [
      {
        title: 'Отправка заявки',
        language: 'tsx',
        code: `const handleSubmit = async (event: React.FormEvent) => {
  event.preventDefault();

  const nextErrors: Record<string, string> = {};
  if (!values.roomId) nextErrors.roomId = 'Выберите помещение';
  if (!values.paymentId) nextErrors.paymentId = 'Выберите способ оплаты';

  const iso = fromRuDate(values.date);
  if (!iso) nextErrors.date = 'Введите дату в формате ДД.ММ.ГГГГ';
  else if (iso < new Date().toISOString().slice(0, 10)) nextErrors.date = 'Дата не может быть в прошлом';

  setErrors(nextErrors);
  if (Object.keys(nextErrors).length) return;

  setSending(true);
  try {
    // Ни статуса, ни userId — их определяет сервер и база
    await api.post('/applications', {
      roomId: Number(values.roomId),
      paymentId: Number(values.paymentId),
      date: iso,
    });
    toast('Заявка отправлена');
    navigate('/cabinet');
  } catch {
    setErrors({ form: 'Не удалось отправить заявку. Попробуйте ещё раз.' });
  } finally {
    setSending(false);
  }
};`,
      },
    ],
    mistakes: [
      {
        title: 'Захардкоженные списки помещений',
        why: 'Задание требует справочники в базе. Список в коде разойдётся с данными и сломает фильтры.',
      },
      {
        title: 'Отправлять дату как ДД.ММ.ГГГГ',
        why: 'MySQL ждёт ГГГГ-ММ-ДД. В базе окажется нулевая дата или ошибка.',
      },
      {
        title: 'Передавать статус с клиента',
        why: '«Изначально заявка имеет статус Новая» — это задача базы, а не формы.',
      },
    ],
    quizId: 'quiz-fullstack-order',
    taskIds: ['task-fs-order-form'],
    projectIds: ['project-05-module-one-run'],
    resources: [],
    examRefs: ['m1-order', 'm2-order-form'],
    prerequisites: ['react-date-input', 'sql-insert'],
    estimatedMinutes: 50,
    planDays: ['day-17-4'],
    source: 'plan',
  },

  {
    id: 'fullstack-cabinet',
    title: 'Кабинет: свои заявки и отзывы',
    tech: ['react', 'express', 'sql'],
    monthNo: 5,
    weekNo: 17,
    importance: 'core',
    summary:
      'Кабинет показывает историю заявок пользователя и даёт оставить отзыв — но только после того, как администратор изменил статус.',
    mustKnow: [
      'Запрос с JOIN и фильтром по пользователю',
      'LEFT JOIN для отзывов',
      'Правило отзыва на клиенте и на сервере',
      'Один отзыв на заявку',
      'Состояния: загрузка, пусто, ошибка',
    ],
    theory: `## Запрос

\`\`\`sql
SELECT a.id, r.title AS room, p.title AS payment,
       a.start_date, a.status, rv.text AS review_text
FROM applications AS a
JOIN rooms AS r ON r.id = a.room_id
JOIN payment_methods AS p ON p.id = a.payment_id
LEFT JOIN reviews AS rv ON rv.application_id = a.id
WHERE a.user_id = ?
ORDER BY a.start_date DESC
\`\`\`

Три важные детали: \`WHERE a.user_id = ?\` со значением из токена, \`LEFT JOIN\` для отзывов (иначе заявки без отзыва пропадут) и понятные псевдонимы столбцов.

## Правило отзыва

Требование модуля 2: «Возможность оставить отзыв у пользователя только после изменения статуса заявки администратором».

**На клиенте** — не показываем форму:

\`\`\`tsx
{application.status !== 'Новая' && !application.review_text && (
  <ReviewForm applicationId={application.id} onSent={reload} />
)}
\`\`\`

**На сервере** — не принимаем запрос:

\`\`\`js
if (rows[0].status === 'Новая') {
  return res.status(400).json({ error: 'Отзыв можно оставить после изменения статуса' });
}
\`\`\`

Обе половины обязательны: первая — про удобство, вторая — про корректность.

## Один отзыв на заявку

Ограничение \`UNIQUE\` на \`application_id\` не даст добавить второй отзыв даже при двойном клике. Дополнительно сервер отвечает 409, а клиент прячет форму, если отзыв уже есть.

## Состояния

\`\`\`tsx
if (loading) return <Spinner />;
if (error) return <ErrorBlock onRetry={load} />;
if (!applications.length) return <p>У вас пока нет заявок</p>;
\`\`\`

## Чек-лист

- [ ] Показываются только свои заявки
- [ ] Видны помещение, дата (ДД.ММ.ГГГГ), оплата и статус
- [ ] Заявки без отзывов не пропадают из списка
- [ ] Форма отзыва не показывается у статуса «Новая»
- [ ] Сервер отклоняет отзыв к заявке со статусом «Новая»
- [ ] Второй отзыв к той же заявке невозможен
- [ ] Есть состояния загрузки, ошибки и пустого списка
- [ ] Коммит`,
    examples: [
      {
        title: 'Проверка правила отзыва',
        language: 'text',
        code: `1. Создать заявку → в кабинете статус «Новая»
   → формы отзыва нет, есть пояснение почему

2. Войти админом, сменить статус на «Мероприятие назначено»

3. Вернуться в кабинет пользователя
   → форма отзыва появилась

4. Отправить отзыв
   → отзыв показан текстом, формы больше нет

5. Проверить сервер напрямую: отправить отзыв к заявке со статусом «Новая»
   fetch('/api/applications/1/review', { method: 'POST',
     headers: { 'Content-Type': 'application/json',
                Authorization: 'Bearer ' + localStorage.getItem('token') },
     body: JSON.stringify({ text: 'тест' }) })
   → ответ 400, в базе ничего не появилось`,
        explanation:
          'Пятый пункт проверяет то, что нельзя увидеть глазами: серверную половину правила. Именно её чаще всего забывают.',
      },
    ],
    mistakes: [
      {
        title: 'INNER JOIN для отзывов',
        why: 'Заявки без отзыва исчезнут из кабинета, и пользователь решит, что данные пропали.',
      },
      {
        title: 'Правило отзыва только на клиенте',
        why: 'Запрос отправляется в обход интерфейса. Требование модуля 2 формально не выполнено.',
      },
      {
        title: 'Показывать все заявки вместо своих',
        why: 'Утечка данных других пользователей. Фильтр по user_id из токена обязателен.',
      },
    ],
    quizId: 'quiz-fullstack-cabinet',
    taskIds: ['task-fs-cabinet-query'],
    projectIds: ['project-05-module-one-run'],
    resources: [],
    examRefs: ['m1-cabinet', 'm2-cabinet-ux'],
    prerequisites: ['sql-join', 'react-effects'],
    estimatedMinutes: 50,
    planDays: ['day-17-5', 'day-17-6'],
    source: 'plan',
  },

  {
    id: 'admin-panel',
    title: 'Панель администратора целиком',
    tech: ['react', 'express', 'sql'],
    monthNo: 5,
    weekNo: 18,
    importance: 'core',
    summary:
      'Вся неделя 18 — на админку: таблица всех заявок, смена статуса без перезагрузки, фильтры, сортировка, пагинация и уведомления.',
    mustKnow: [
      'Защита маршрута на сервере',
      'Таблица с JOIN по всем справочникам',
      'Смена статуса и обновление одной строки',
      'Фильтр, сортировка, страницы',
      'Уведомление и подтверждение',
    ],
    theory: `## Порядок работы за неделю

\`\`\`text
День 1  Таблица всех заявок (JOIN) + защита маршрута
День 2  Смена статуса через PATCH без перезагрузки
День 3  Фильтр по статусу
День 4  Сортировка по дате кликом по заголовку
День 5  Постраничная навигация по 5 записей
День 6  Уведомление и окно подтверждения
День 7  Проверка по чек-листу задания
\`\`\`

Каждый день заканчивается коммитом — к концу недели их семь.

## Защита

\`\`\`js
app.use('/api/admin', requireAuth, requireAdmin, adminRouter);
\`\`\`

Подключение защиты ко всей группе надёжнее, чем к каждому маршруту: новый админский маршрут нельзя случайно оставить открытым.

## Смена статуса

\`\`\`tsx
const changeStatus = async (id: number, status: Status) => {
  try {
    await api.patch(\`/admin/applications/\${id}\`, { status });
    setApplications((prev) => prev.map((a) => (a.id === id ? { ...a, status } : a)));
    toast('Статус изменён');
  } catch {
    toast('Не удалось изменить статус', 'danger');
  }
};
\`\`\`

Обновляется одна строка в состоянии — без перезагрузки всего списка.

## Инструменты модуля 2

Требование дословно: «фильтры, всплывающие окна уведомлений, постраничная навигация и возможность сортировки данных». Все четыре пункта должны быть видны на экране.

## Подтверждение

Программа предлагает модальное окно «Точно сменить статус?». Это не обязательное требование задания, но действие необратимо — окно уместно и добавляет проекту законченности.

## Чек-лист

- [ ] Админка доступна только по роли admin (и на клиенте, и на сервере)
- [ ] Видны ФИО, телефон, помещение, дата, оплата, статус
- [ ] Статус меняется на «Мероприятие назначено» и «Мероприятие завершено»
- [ ] Сервер отклоняет любой другой статус
- [ ] Таблица обновляется без перезагрузки
- [ ] Фильтр по статусу
- [ ] Сортировка по дате
- [ ] Постраничная навигация по 5 записей
- [ ] При смене фильтра страница возвращается на первую
- [ ] Всплывающее уведомление после смены статуса
- [ ] Таблица не ломается на 390 px
- [ ] Коммит`,
    examples: [
      {
        title: 'Маршрут смены статуса',
        language: 'javascript',
        code: `const ALLOWED_STATUSES = ['Мероприятие назначено', 'Мероприятие завершено'];

router.patch('/applications/:id', async (req, res) => {
  const id = Number(req.params.id);
  const { status } = req.body ?? {};

  if (!Number.isInteger(id)) {
    return res.status(400).json({ error: 'Некорректный идентификатор' });
  }
  if (!ALLOWED_STATUSES.includes(status)) {
    return res.status(400).json({ error: 'Недопустимый статус' });
  }

  try {
    const [result] = await pool.execute(
      'UPDATE applications SET status = ? WHERE id = ?',
      [status, id],
    );

    if (result.affectedRows === 0) {
      return res.status(404).json({ error: 'Заявка не найдена' });
    }

    res.json({ id, status });
  } catch (error) {
    console.error(error);
    res.status(500).json({ error: 'Не удалось изменить статус' });
  }
});`,
        explanation:
          'Маршрут уже находится внутри группы с requireAuth и requireAdmin, поэтому здесь остаётся только проверка данных.',
      },
    ],
    mistakes: [
      {
        title: 'Принимать любой статус',
        why: 'Задание допускает ровно два перехода. Без белого списка в базе окажется что угодно.',
      },
      {
        title: 'Перезагружать весь список после смены статуса',
        why: 'Лишний запрос и мигание таблицы. Достаточно обновить одну строку в состоянии.',
      },
      {
        title: 'Таблица шире экрана на телефоне',
        why: 'Модуль 2 требует совместимости с 390 px. Помогает table-responsive.',
      },
    ],
    quizId: 'quiz-admin-panel',
    taskIds: ['task-react-admin-table', 'task-fs-admin-status'],
    projectIds: ['project-05-module-one-run'],
    resources: [],
    examRefs: ['m1-admin', 'm2-admin-tools'],
    prerequisites: ['react-admin-ui', 'auth-roles'],
    estimatedMinutes: 55,
    planDays: ['day-18-1', 'day-18-2', 'day-18-6'],
    source: 'plan',
  },

  {
    id: 'ui-polish',
    title: 'Модуль 2 на живом проекте: внешний вид и мобильная версия',
    tech: ['css', 'design'],
    monthNo: 5,
    weekNo: 20,
    importance: 'core',
    summary:
      'Полтора часа второго модуля тратятся не на переделку, а на доводку: подсказки у полей, слайдер, экран 390×844, оптимизация изображений.',
    mustKnow: [
      'Что именно оценивают в модуле 2',
      'Порядок работы за 90 минут',
      'Чек-лист мобильной версии',
      'Оптимизация изображений',
      'Когда остановиться',
    ],
    theory: `## Что оценивают

Дословно из задания: оптимизация изображений, значки, поля, кнопки, ссылки, совместимость с 390×844, слайдер из четырёх изображений с автопереключением каждые три секунды, подсказки об ошибках, предупреждения при входе, удобный вывод заявок, выпадающие списки, дата ДД.ММ.ГГГГ, фильтры, уведомления, пагинация, сортировка.

## Порядок за 90 минут

\`\`\`text
0–10    Открыть архив Прил_…М2.zip, посмотреть материалы
10–25   Слайдер: 4 изображения, 3 секунды, кнопки       → коммит
25–40   Подсказки об ошибках во всех формах             → коммит
40–55   Админка: фильтр, сортировка, пагинация, тосты   → коммит
55–75   Мобильная версия: все страницы на 390×844       → коммит
75–90   Единый вид: отступы, цвета, иконки, анимации    → коммит
\`\`\`

Слайдер идёт первым: он самый заметный и самый проверяемый пункт.

## Чек-лист мобильной версии

Откройте DevTools, режим устройства, 390×844 и пройдите все пять страниц:

- [ ] Нет горизонтальной прокрутки
- [ ] Меню сворачивается в бургер
- [ ] Формы занимают ширину экрана
- [ ] Таблица админки прокручивается или превращается в карточки
- [ ] Кнопки не меньше 44 px в высоту
- [ ] Текст читается без увеличения
- [ ] Слайдер не занимает пол-экрана
- [ ] Модальные окна помещаются

## Изображения

Задание отдельно называет «оптимизацию изображений». Практически:

- уменьшить исходники до нужного размера (не 4000 px для блока в 600);
- сжать (JPG с качеством 75–85);
- привести к одному размеру через \`object-fit: cover\`;
- поставить \`width\` и \`height\` в разметке;
- \`loading="lazy"\` для всего, кроме первого экрана.

## Когда остановиться

Полтора часа — мало. Лучше аккуратно закрыть все пункты задания, чем сделать один экран идеальным, а остальные бросить. Критерий простой: **каждый пункт из списка требований должен быть виден на экране**.

Не тратьте время на: собственную иконочную библиотеку, сложные анимации, тёмную тему, идеальную типографику. Это не оценивается.`,
    examples: [
      {
        title: 'Быстрая проверка всех страниц',
        language: 'text',
        code: `F12 → Ctrl+Shift+M → Responsive → 390 × 844

/login      форма по центру, ссылка на регистрацию видна
/register   пять полей, подсказки под полями помещаются
/order      выпадающие списки во всю ширину, дата вводится
/cabinet    слайдер не растянут, карточки заявок читаются
/admin      таблица прокручивается, фильтр и пагинация доступны

На каждой странице прокрутить вниз до конца и убедиться,
что горизонтальной полосы прокрутки нет.`,
      },
    ],
    mistakes: [
      {
        title: 'Начинать модуль 2 с переделки дизайна',
        why: 'Полтора часа уйдут на один экран. Сначала закройте перечисленные в задании пункты.',
      },
      {
        title: 'Проверять адаптив только сужением окна',
        why: 'Окно редко доходит до 390 px. Нужен режим устройства.',
      },
      {
        title: 'Забыть про архив приложений',
        why: 'В Прил_…М2.zip могут быть картинки и тексты, которые сэкономят время.',
      },
    ],
    quizId: 'quiz-ui-polish',
    taskIds: ['task-css-responsive-390', 'task-css-object-fit'],
    resources: [],
    examRefs: ['m2-design', 'm2-mobile', 'm2-slider'],
    prerequisites: ['css-responsive', 'react-slider', 'design-basics'],
    estimatedMinutes: 50,
    planDays: ['day-20-1', 'day-20-4', 'day-20-6'],
    source: 'plan',
  },
];
